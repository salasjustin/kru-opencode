# Roster — v0.82.0

The lead is the `lead` skill (`/kru/lead`, runs in the main thread). It delegates to the specialists below and to built-in agents (`general`, `plan`) and skills (`kru-tdd`, `kru-diagnosing-bugs`, `kru-testing`, `kru-local-browser`) — review routes to the `kru/code-reviewer` seat, which is this fork's replacement for claude's bundled review skill. Every specialist follows **official sources first** (`SOURCES.md`).

## Scope — where this team stops
Engineering-led, cross-functional at the top. The team spans **product-development**: engineering (core) + design + the thin upstream layer that feeds the build. **The user is the PM** — there is no product-management function, no roadmap, and no prioritization framework in the team; what-to-build-next is the user's call, and the team formalizes and executes it. The upstream layer is two things: the lead's **`brief` verb** (grill → a change-shaped `brief.md` incl. the commit/PR cadence, inline) and `planner`, an **upstream adapter** that turns that brief into a spec/ticket graph handing *into* engineering. Neither runs a separate org. `ux-designer`/`graphic-designer` feed the build; growth on shipped pages is user-invoked — `/kru/seo-review` for technical SEO/AEO, `/copywriting`/`/copy-editing`/`/cro` for conversion copy.

The team stops at product-development. **Company functions are out of scope and stay uninstalled**: sales, marketing **channels/campaigns** (email, ads, social, SMS, PR, outreach, referrals), finance, legal, support, ops/HR. The line is *what ships in the build vs. what runs as a channel*: on-page conversion copy + CRO on the pages the team builds is product-development (the user-invoked `/copywriting`/`/copy-editing`/`/cro` passes), the same way technical SEO on those pages is (the user-invoked `/kru/seo-review` pass); a campaign run through a channel is not. This is the same discipline the vendoring rule runs on (vendor only the upstream skills that feed the build; don't drag in whole plugins). Adding a seat is fine (see *Growing the team*); adding a *business function* is a different product — don't.

## Current specialists (`agents/`)
| Agent | Role | Backing source |
|---|---|---|
| `ux-designer` | Experience layer upstream of the look: user research, user flows + IA, flow critique, UX copy, **the conventions file in both eras** (pre-look corpus → shipped-system header once a system exists), design→eng handoff spec (artifacts, no code). On greenfield its screen inventory + conventions corpus are what **the lead briefs the design canvas with**; on brownfield it is **the whole upstream pass** — a feature there routes `ux-designer` → builder with no design hop | vendored `design`-plugin skills: `user-research`, `research-synthesis`, `ux-copy` |
| `ui-designer` | The **canvas turn** and the **coverage ledger**: drafts the `.dc.html` artboards a design canvas is seeded from (2–4 directions at bootstrap, screen mockups after), publishes it, re-seeds it on every later change, and keeps `design-system.md` — the element × state ledger a build is dispatched against. Runs in its own context so ~2 MiB of artboard markup never lands in the lead's, which keeps the **user channel** — the direction question and the verdict — with the lead | bundled `kru/ui-designer` skill (the design turn's canvas editor) |
| `ux-auditor` | Walks a **named user flow** statically through source (route → loader/action → components → links/branches → terminal/error/empty/loading states) and audits the path against canonical UX principles; reports findings + fixes, **no code**. Journey-scoped, where `visual-reviewer` is screen-scoped and `accessibility-reviewer` is conformance-scoped | repo-authored `ux-principles` skill |
| `graphic-designer` | Generates/enhances web-ready image assets + ambient hero video (Veo) + code-generated generative art (p5.js) + true-alpha cutouts (rembg) from the plan | `scripts/gen-asset.ts` (`@google/genai`, ffmpeg, rembg) + vendored `algorithmic-art` skill + Context7 |
| `sveltekit-builder` | SvelteKit **network boundary**: routing, `load`, form actions, hooks, endpoints, thin `+page.svelte` mounts — components come from `svelte-ui-builder` | Svelte MCP + `svelte:*` skills (official AI surface) |
| `react-router-builder` | React Router (framework/data/declarative/RSC) **network boundary**: route modules, loaders, actions, fetchers, typegen — components come from `react-ui-builder` | vendored official `react-router` skill → installed docs → Context7 |
| `nextjs-builder` | Next.js App Router **network boundary**: Server-Component data fetching, Server Actions, route handlers, layouts, caching, middleware — components come from `react-ui-builder` | `vercel:nextjs` + `vercel:*` skills + Vercel MCP → Context7 |
| `tanstack-start-builder` | TanStack Start **network boundary**: file routes, `createServerFn` RPCs, server routes, middleware, loaders/`beforeLoad`, search-param validation, SSR/RSC — components come from `react-ui-builder`. Also the routing seat for a Start-less `@tanstack/react-router` SPA | TanStack's own skills, shipped **inside the installed packages** (`node_modules/@tanstack/*/skills/`) → `tanstack.com/start/latest/llms.txt` → Context7 |
| `go-fullstack-builder` | **Go-served React app**, the whole request path in one seat: the Go HTTP service (`net/http` method+pattern routing, handlers, middleware, the JSON contract + field-error map, `http.Server` timeouts + graceful shutdown, the built SPA embedded via `embed` with the SPA-fallback/JSON-404 split) **and** the React side of that wire (typed api client, query hooks, route-level glue) — components come from `react-ui-builder`, schema from the data architects. The seam vs. the four framework builders is the **server language**: a Node-served React app is theirs. Auth and billing are in-seat here (the two specialists hand a Node surface), which is why the boundary carries the security hard-rigor clause | `go doc` off the installed toolchain + `go.dev` (Effective Go, spec, module docs) + `pkg.go.dev` → Context7 |
| `python-developer` | **The Python layer**, whole: MCP servers on the official `mcp` SDK (tool surface, error contract, trust boundary), libraries and their public API, CLIs and scripts, the `pyproject.toml` that packages them and the `ruff`/`mypy`/`pytest` gate that guards them. The seam vs. the framework builders and `go-fullstack-builder` is the **language of the artifact** — Python code and its packaging are this seat's wherever they sit in the repo; a Node or Go service is theirs. Schema stays the data architects' (the Python that runs the queries is this seat's), and the JS-side task graph, formatter and linter stay `toolchain-engineer`'s — the `[tool.ruff]`/`[tool.mypy]` config in `pyproject.toml` is this seat's, the same way `gofmt`/`go vet` are the Go seat's | `python -m pydoc` off the installed interpreter + `docs.python.org` + the installed package's own source → `modelcontextprotocol.io` / PyPI → Context7 |
| `react-ui-builder` | Framework-agnostic React UI: pages/sections/interactive components as props-in/callbacks-out `.tsx`, mounted by the RR7/Next/Start boundary seats or `go-fullstack-builder`'s route glue. Carries the UI-craft skill load (`ark-ui`, `modern-css`, `vercel:react-best-practices`, design tokens) so the framework seats stay lean | `vercel:react-best-practices` + `ark-ui`/`modern-css` skills → Context7 |
| `svelte-ui-builder` | Framework-agnostic Svelte 5 UI: pages/sections/interactive components as props-in/callbacks-out `.svelte`, mounted by the SvelteKit boundary seat. Carries the UI-craft skill load (`ark-ui`, `modern-css`, design tokens) | Svelte MCP + `svelte:*` skills + `ark-ui`/`modern-css` skills |
| `web-components-builder` | Framework-agnostic UI as **vanilla custom elements** with a shadow root — an embeddable widget dropped into a page the team doesn't control, or a design system consumed by more than one stack. Owns the element's **public API** (attributes in, events out, `part`/custom-property theming contract), its shadow root, styling and a11y. The seam vs. the other two UI builders is the **consumer**, not the markup: UI inside a React or Svelte app is theirs; UI that has to outlive a framework is this one's | repo-authored `web-components` skill + `modern-html`/`modern-css` + MDN/`api.webstatus.dev` (the platform is the source) |
| `sanity-builder` | Sanity content model, GROQ, TypeGen, integration | `sanity:*` skills + Sanity MCP |
| `cloudflare-builder` | Cloudflare edge-runtime builder: Workers, Wrangler config/deploy, bindings + storage (KV/D1/R2/Queues/Vectorize), Durable Objects, Pages, framework-on-Workers adapter. Same hand-to-framework-builder pattern for the CF runtime; **owns D1** (CF's SQLite) — the binding, migration CLI, and request model are Workers-shaped, so it isn't `sqlite-architect`'s embedded-file lane | vendored `cloudflare` + `wrangler` skills + Cloudflare MCP + Context7 |
| `vercel-platform-engineer` | Vercel platform-ops: `vercel.json`, deploy/CI-CD, env/secrets, Functions/edge runtime, Cron, domains, Firewall/WAF, AI Gateway, Marketplace/storage provisioning, Routing Middleware. **Not** app code (`nextjs-builder`) or CWV/caching-for-speed (`vercel-perf-optimizer`) — the deploy/infra/security lane | `vercel:*` skills (deployments-cicd/vercel-cli/env-vars/vercel-functions/vercel-firewall/…) + Vercel MCP |
| `vercel-perf-optimizer` | Web perf: CWV, caching, rendering, bundle (post-build) | `vercel:performance-optimizer` + `vercel:*` skills + Vercel MCP |
| `fly-platform-engineer` | Fly.io platform-ops: the `Dockerfile`/image, `fly.toml`, Machines + scaling, Volumes, secrets/env, deploy strategy + `release_command`, regions, private networking + certs, resource provisioning (MPG/Tigris/Upstash). The lane the other two platform seats structurally can't hold: a **long-lived Firecracker VM with an attached persistent volume**, where Vercel Functions and CF Workers are request-scoped — a container that has to be a container, a background worker, scale-to-zero with a disk. **Not** app code (the framework builder) or schema on Fly MPG (`postgres-architect`) | flyctl MCP (`fly mcp server`) + `fly.io/llms.txt` + docs → Context7 |
| `postgres-architect` | Schema, migrations, typed query surface — **a Postgres server**, client/server and multi-writer | Context7 |
| `sqlite-architect` | **An embedded `.db` file** the app opens directly (CLI, desktop, local-first, OSS lib): connection pragmas, STRICT schema, `IMMEDIATE`-transaction concurrency, the 12-step rebuild + `user_version` migrations, backups/WAL upkeep, driver choice. The seam vs. `postgres-architect` is the deployment shape, not the SQL — one writer, per-connection settings, advisory types, and a file that ships in users' hands | repo-authored `sqlite` skill + sqlite.org (engine) + Context7 (drivers) |
| `better-auth-specialist` | Auth layer: server config, adapter + CLI schema, plugins (2FA/passkey/org/social/SSO), sessions/cookies, typed client. Hands a typed auth surface to the framework builder | Better Auth docs MCP → official Skills → `llms.txt` → Context7 |
| `stripe-specialist` | Payments + billing: server SDK client, product/price catalog, Checkout Sessions, Billing/subscriptions, Connect, the signature-verified webhook handler, and the persisted entitlement state the app gates on. Hands a typed billing surface to the framework builder. Identity stays `better-auth-specialist`'s — this seat maps an authenticated user to a Stripe customer | `docs.stripe.com/llms.txt` → the `.md` twin of any docs page → `stripe/stripe-node` on GitHub → Context7. **Deliberately no MCP** — Stripe's docs are fully static markdown |
| `paypal-specialist` | Payments + billing on **PayPal**: the server REST client and its token cache, Orders v2 checkout/capture, Catalog Products + Billing Plans + Subscriptions, refunds, the signature-verified webhook listener, and the settlement state the app persists. Owns the browser JS SDK session surface too (eligibility, `onApprove` → capture) and hands the builder a typed payment surface. Stripe is `stripe-specialist`'s — a repo may run both rails, and neither object graph transfers | `developer.paypal.com/llms.txt` (an index of indexes) → the `.md` twin of any docs page → `paypal/paypal-rest-api-specifications` → the official SDKs → Context7. **The official MCP is merchant operations, not docs** — it acts on a real account, so it is no rung on the chain |
| `code-reviewer` | Adversarial correctness/quality review | Context7 per stack |
| `architecture-reviewer` | Structural integrity: seams, interface depth, coupling (design-time + review-time) | `codebase-design` skill |
| `visual-reviewer` | Coverage pass on the **rendered** UI in a live browser: the states and viewports nobody ever renders (empty, error, loading, disabled, focus-visible, 375, the content extremes), what breaks in them, and the **cause** behind a visible defect traced to `file:line` with its scope. Same two-callers-one-body shape. It reports breakage and causes, **never a verdict on intent** — dispatching it buys the states the user never opened, not the design gate. **Numbers are evidence, not output**: one measurement where it settles a doubt, never a table — tap targets are `accessibility-reviewer`'s, off-token values the repo gate's, contrast ratios nobody's (the design settles them) | `visual-review` skill (→ `local-browser`) |
| `accessibility-reviewer` | WCAG 2.1 AA conformance audit of a built page/component: keyboard operability, focus order + visibility, target size, name/role/value, labels and error identification. **Color contrast (1.4.3/1.4.11) is the design's and ships as authored** — no seat on this team computes a ratio, so the audit names it unassessed rather than reading as a full-AA claim. **A seat over the vendored skill**, so the pass answers to both a lead and a user caller. Measured via `local-browser` when a dev server is up; static from source otherwise, with every rendered criterion marked *not verified* rather than passed | vendored `accessibility-review` skill (→ `local-browser`) |
| `test-writer` | Coverage sweeps + fan-out, suite repair, the exempt seats' output, and discovering/capturing an unfamiliar repo's testing conventions. **Not** first coverage of a behavior seat's work — that's written test-first inside the build; a builder returning untested work with no exemption named comes back as reported drift, not a silent backfill | `testing` + `tdd` skills + Context7 per runner; project testing skill/docs |
| `toolchain-engineer` | Repo tooling substrate (not app code): pnpm workspace/package graph → Turborepo task graph + caching → Biome (or ESLint/Prettier) lint/format. Owns `pnpm-workspace.yaml`/`turbo.json`/`biome.json` + the lint/format/build/typecheck/test **task** wiring. Seam with the `typescript` skill (that owns `tsconfig` *content*; this owns the tasks that run it) | vendored `turborepo` skill + pnpm/Biome official docs + Context7 |
| `planner` | Persisted plan of record for work bigger than one context: spec, tracer-bullet ticket graph, or wayfinder map, in user-level files (AFK synthesis+publish; lead owns the human loop) | vendored `to-spec`/`to-tickets`/`wayfinder` skills + `TRACKER.md` (files under the store's `plan/`) |
| `dispatch-auditor` | Process audit of the lead's own orchestration — the one surface no other reviewer watches. Reads the session's dispatch ledger (`hooks/log-dispatch.sh` logs every team-seat `Agent` call to `~/.claude/kru/audit/<session>.jsonl`; `hooks/nudge-audit.sh` blocks a stop once while a ledger stands and hands back the dispatch instruction) and checks it against the `lead` Step 3 contract: routing fit, handoff completeness, grouping/reuse, ambient-block restatement, and unverified returns (a build seat owing Block O's `Return pass:` line whose return carried none — the ledger's `block_o`/`return_pass` pair). Files durable deviations as `[workflow]` inbox lines — the evolution loop's third writer; `/kru/roster learn` stays the gate, so autonomy lives in the capture, never in the edit. Deletes the ledger as its completion mark (absence == audited). **Hook-invoked** — reaches a session via the Stop nudge, absent from `lead` routing by design. Stack-agnostic (its rulebook is the team's own lead contract), so no `SOURCES.md` row | `lead` SKILL.md → Step 3 (the contract it audits against) |

## Model tiers
Every non-lead seat is pinned to an explicit `model:` in its frontmatter — no seat inherits the session model. Policy — spend on correctness (implementers, judgment-heavy specialists, the hardest reviewers), economize where the work is pattern-matching/checklist application.

**Why pinned instead of inherit**: under `inherit` the team's cost scales with the *lead's* tier rather than each seat's actual need — a Fable 5 (premium, $10/$50) lead session drags all 25 subagent calls onto Fable pricing, the mechanical ones included. Pinning decouples them: the lead runs on any tier while specialists cost what their work requires.

**Why full model IDs instead of the `opus`/`sonnet` aliases**: the aliases float to whatever the current generation resolves to, so an install's behavior silently changes under it. Every seat names `claude-opus-5` / `claude-sonnet-5` outright — reproducible across machines, plans, and the web VM. Cost: an install whose plan or provider (Bedrock/Vertex/Foundry) doesn't expose those IDs must swap back to the alias.

| Agent(s) | `model:` | Why |
|---|---|---|
| `lead` skill (`/kru/lead`) | inherit (main thread) | routes/integrates/verifies on the session model. Not settable via frontmatter |
| `ux-designer`, `ux-auditor`, `ui-designer`, `graphic-designer`, all `*-builder` (incl. `cloudflare-builder` and the UI component builders `react-ui-builder`/`svelte-ui-builder`/`web-components-builder`), `postgres-architect`, `sqlite-architect`, `better-auth-specialist`, `stripe-specialist`, `paypal-specialist`, `python-developer`, `vercel-platform-engineer`, `fly-platform-engineer`, `test-writer`, `planner`, `code-reviewer`, `architecture-reviewer`, `visual-reviewer`, `accessibility-reviewer` | **`claude-opus-5`** (pinned) | code correctness + design judgment; opus is the coding/judgment floor regardless of session tier. `ux-designer` sets flows/IA/copy the whole build inherits — research synthesis + interaction judgment want the top tier. `better-auth-specialist` writes security-critical auth config — a wrong default (disabled verification, permissive origins) is expensive. `stripe-specialist` writes the code that moves money — an unverified webhook, a client-supplied amount, or a non-idempotent fulfillment is a charge you can't take back. `paypal-specialist` carries that same blast radius on a surface with more ways to get it silently wrong: four generations of REST API share webhook event **names** while carrying different resource shapes, so the branch that looks right is routinely reading the wrong object, and settlement arrives out of order by design. `graphic-designer` needs opus-level prompt craft + slop curation. `ui-designer` drafts the artboards a look is picked from and reads coverage against the inventory — direction variance and "can every screen be composed from this" are judgment calls, and a canvas the user rejects costs a whole turn. `test-writer` writes real code + debugs failures. `planner` decomposition quality gates all downstream parallelism. `cloudflare-builder` writes real edge-runtime code + bindings — a wrong binding/DO or `nodejs_compat` assumption fails at deploy. `vercel-platform-engineer` owns deploy/env/firewall config — a leaked secret or fail-open rule is expensive; security-adjacent. `fly-platform-engineer` carries that same blast radius one platform over, with more of it irreversible — a `fly secrets set` restarts live Machines, a Volume holds the **only** copy of its data, and the managed-vs-unmanaged Postgres call it makes on the user's behalf hands them an operations burden they can't hand back. `code-reviewer`/`architecture-reviewer` must never run below the builders they gate — adversarial bug-finding and seam/coupling review are the hardest judgment calls on the team. `ux-auditor` is one invented finding away from being untrusted — holding the `STATIC`/`HEURISTIC`/`RENDERED` line means refusing to claim what the source doesn't prove, which is exactly the restraint a cheaper tier drops. `web-components-builder` writes a **public API other people's pages depend on** — a tag name, an attribute set, an event shape and a `part` contract can't be renamed once a third-party site embeds them, and the platform failures it has to hold in mind (a property shadowing its accessor before upgrade, an event that never leaves the shadow root, ARIA that can't cross it) are all silent. `sqlite-architect` migrates a file the team can't roll back — it lives on users' disks, so a bad rebuild step is unrecoverable. `go-fullstack-builder` owns both sides of a public wire plus the sessions and CSRF a Go server does for itself — a permissive decoder, a missing timeout or a cookie without `HttpOnly` is silent until it's exploited. `python-developer` writes the tools an LLM calls with attacker-influenced input, and Python's own gate is the weakest of any stack the team builds on — nothing at runtime enforces an annotation, `ruff` and `mypy` both pass a `0` swallowed by a truthiness guard, and the MCP surface it owns is a prompt-injection boundary from its first tool. The three screen passes sit on the same rule as `ux-auditor` and for the same reason: each is **one fabricated finding away from being untrusted**, and the restraint they run on is what a cheaper tier drops first — `accessibility-reviewer` must decide *which surface it's reviewing* and report a scope-skipped criterion as skipped rather than passed, across a checklist carrying both a surface split and a token-conformance half; `visual-reviewer` must hold the coverage/judge line under constant temptation to say "this doesn't look right," and it both reads screenshots and traces what they show back to a `file:line`, so it needs the vision tier and the tracing one; `accessibility-reviewer` must not invent a contrast ratio it never computed — a fabricated number is indistinguishable from a measured one downstream |
| `toolchain-engineer`, `vercel-perf-optimizer`, `dispatch-auditor` | **`claude-sonnet-5`** (pinned) | mechanical/checklist work: wiring config against documented pnpm/Turborepo/Biome conventions, and applying known CWV/caching playbooks respectively — sonnet is enough and keeps the highest-volume review/apply calls cheap. `dispatch-auditor` is rule-check of a compact ledger against a fixed four-class contract, and it fires at every turn-end that dispatched a seat — the highest-frequency dispatch on the team, so it must stay the cheapest; its filing bar (durable, patterned, ≤3 lines) does the restraint work a pricier tier would otherwise carry |

**Effort is pinned where the output *is* a judgment, and inherited everywhere else.** `effort:` in agent frontmatter takes `low`|`medium`|`high`|`xhigh`|`max`. `code-reviewer`, `architecture-reviewer`, `ux-auditor` and `planner` pin **`high`**: what they return is a finding set or a decomposition, so how hard the model looked *is* the product — a session dropped to `medium` silently shortens the list, and a short list reads exactly like a clean diff. The sonnet tier (`toolchain-engineer`, `vercel-perf-optimizer`, `dispatch-auditor`) pins **`medium`**: checklist application against documented conventions, which a high-effort session shouldn't pay premium thinking for. Every other seat inherits the session level deliberately — unlike model tier, effort tracks how hard *this change* is, and a builder executing a settled spec should move with the session rather than against it. `xhigh`/`max` error out when thinking is disabled, so no seat names them.

**Prompt cache TTL: `1h` on the seats the lead re-dispatches.** `experimental.cacheTtl: "1h"` on the eight builders, `python-developer`, `test-writer` and `code-reviewer` — the seats one lead run calls repeatedly, minutes apart. A 1h cache write costs more than a 5m one and is repaid on the second dispatch, so the seats called once per change (`planner`, `ui-designer`, `stripe-specialist`, `paypal-specialist`, `better-auth-specialist`, the platform seats) stay on the 5m default where a single write is the whole story. A user-level `subagentPromptCacheTtl` setting overrides the frontmatter, and `1h` is ignored while a Claude subscription is in overage.

Fable 5 is the premium tier ($10/$50, above Opus 4.8's $5/$25). Don't pin any seat to it. Want the whole team a tier cheaper (or pricier) across the board? Re-pin every `claude-opus-5`/`claude-sonnet-5` line above in one pass rather than reverting to `inherit`, which reintroduces the lead-tier coupling above.

## Reused, not owned
Built-ins: `general` (codebase mapping), `plan` (architecture).
Repo-authored shared skills:
- **`typescript`** (`skill/kru-typescript/` — cheat-sheet baseline + type craft + compiler-config discipline). Loaded by every TS-writing builder (`*-builder`, `better-auth-specialist`, `postgres-architect`, `test-writer`) — **TypeScript is ambient**: typing lives in-place in whichever seat owns the code, one clean seam, zero extra hops. Repo-wide TS-infra work (strict migration, monorepo project references, type-perf profiling) = the lead dispatches a general agent with this skill; mint a dedicated seat only if that recurs. Excludes the formatter/linter **and the monorepo task/package graph** (Biome/ESLint/Prettier + pnpm + Turborepo) — now the **`toolchain-engineer`** seat's; the skill owns `tsconfig` content, the seat owns the tasks that run it.
- **`ark-ui`** (`skill/kru-ark-ui/` — headless interactive primitives on Zag.js: reach-for-Ark judgment, part anatomy, built-in a11y, styling-token hookup). Loaded by the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`) — **Ark UI is ambient**: the seat that owns the component composes the accessible primitive in-place, one owner end-to-end. Same ambient-craft rationale as `typescript`; brownfield defers to the repo's existing component lib — and where that lib is **shadcn**, the vendored **`shadcn`** skill (`skill/kru-shadcn/`, shadcn's own, `react-ui-builder` only) is the playbook. Two primitive libraries, one per repo: the repo decides brownfield (`components.json`), the lead's plan decides greenfield React, and the rule is written once in `ark-ui`'s reach-for section so the two skills can't drift apart. The post-build a11y audit is `accessibility-reviewer`'s (or the user's `/kru/accessibility-review`) — a **catch**, never the net the build leans on: a criterion that keeps failing is a builder-prompt problem, and the seat is told to say so.
- **`visual-review`** / **`accessibility-review`** (`skill/kru-visual-review/`, `skill/kru-accessibility-review/`) — the two **screen passes**, each with **two callers and one body**. The skill file *is* the pass; the seat (`visual-reviewer` / `accessibility-reviewer`) is a thin frontmatter that `read`s that file and executes it, so there is no forked copy to drift. `disable-model-invocation` stays on both skills — that keeps `/kru/visual-review` etc. the **user's** front door and stops anything firing them ambiently — and the lead reaches the same pass by spawning the seat, in the Step 4 parallel batch. Report-only and intent-conserving either way: findings relayed unsoftened, and the seats never edit at all (no user channel, so "apply fixes if asked" has nobody to ask).
  **What the seats change and what they don't.** They change *who may call a pass* — a blanket "every review pass is user-invoked" costs the lead the two mechanical gates it should always have, and makes the two mechanical passes reachable only if the user remembers to ask. They do **not** move the judge line: `visual-reviewer` still **does not judge intent**, which is the whole reason a browser sweep can't stand in for the design gate. So conformance is still **prevented** (an authoritative, closed token file plus a handoff that names the set, with builders returning named gaps instead of inventing values), **enforced by the repo's own gate** at every commit, and **judged by the user**. Dispatching a pass buys coverage and rule violations; it never buys the design gate.
  **What `visual-reviewer` is actually for — and it isn't a second eye on the look.** The user has already looked at the page, in their own window, on the happy path, and beats any agent at *does this look right* in a glance. Two things that look never covers are the seat's whole value: **coverage** — the empty state, the error state, loading, disabled, focus-visible, 375, the row with the 90-character string, which in most projects nobody has ever rendered, so "this state exists and no one has seen it" is a finding on its own — and **cause**, turning a defect the user spots in a second into a `file:line` with a scope, which is the hour they'd otherwise spend. Everything else it used to do went to the seat that owns it: tap targets are a WCAG criterion (`accessibility-reviewer`, which drives the same browser), and a value outside the token file is a grep — now the repo's own conformance gate, a hundred times cheaper there than in a browser. Contrast went further out still — it is **the design's**, audited by no seat, since re-deriving it downstream re-opens a decision whose only fix is a redesign. What's left of measurement is **one number inline where it settled a real doubt** — never a table, which was mostly the pass restating what the token file already said.
  **A second vision model reading the captures is rejected**, on its own test: a Gemini pass over this seat's screenshots missed an obvious visible defect, and an impression channel that can miss the obvious is worse than none — its read is worded the same whether or not it saw anything, so what it buys is confidence rather than coverage. The design gate is the user's glance; the way to improve on it is to render what a glance never reaches, not to buy a second, worse one.
  The passes that stayed the user's — `/kru/seo-review`, `/copywriting`, `/copy-editing`, `/cro`, `/kru/review-animations`, `/kru/improve-animations` — are the ones carrying **taste** rather than a checkable rule, and a dispatched taste pass is the team quietly editing the user's voice.
- **`ux-review`** (`skill/kru-ux-review/`) — the **journey pass's user front door**, and the two-callers rule with the halves swapped: the body stays in the **`ux-auditor`** seat (the walk's rules already live there and in `ux-principles`' `Detect:` tiers), so the skill is the **caller's** half — which flow to walk, the spawn, and the relay. The relay exists because the seat has **no user channel**: the resolved entry route leads it (a wrong entry voids every finding after it, and one line fixes it), `HEURISTIC` matches stay questions rather than reaching a builder as a fix list, and fixes wait to be asked for. `disable-model-invocation` like the screen passes — the lead reaches the same pass by spawning the seat, and nothing fires the command ambiently. **Multi-screen is the bar**: one screen is `/kru/visual-review`'s and `/kru/accessibility-review`'s, and nothing else on the team walks *between* screens.
- **`seo-review`** (`skill/kru-seo-review/`) — **self-contained user-invoked audit skill**: `/kru/seo-review` runs the technical SEO/AEO sweep (indexability, discoverability, metadata/OG/JSON-LD, hreflang, AI-answer readiness) **inline in the main thread**, backed by the `sanity:seo-aeo-best-practices` checklist + the stack's meta API. Report-first by layer; minimal markup fixes applied only when the user asks. It stays **user-only** even though the three screen passes carry seats — technical SEO is judged against a page's goals, not a checkable rule the lead can gate on; CWV stays `vercel-perf-optimizer`'s.
- **`brief`** (`skill/kru-brief/`) — **self-contained user-invoked planning skill**, promoted off the lead's verb list in v0.51.0 for the reason `todo`/`issue` were: it is typed, it writes one file, and it needs none of routing, the handoff contract, the gates or reconcile — so typing it charged the whole orchestration body to write one file, and the verb form cost the lead a `brief`-versus-task disambiguation paragraph that an address deletes. `/kru/brief <subject>` grills the user (`/grilling`) and then **persists a change-shaped `brief.md`** — `Change` · `Why now` · `Blast radius` · **`Landing plan`** (the cadence — commits are the steps; a PR split means an environment boundary or a partial ship) · `Decisions resolved` · `Non-goals` · `Done when` — into the user-level plan store, rewritten in place on each re-grill. It replaced the `/pm` pass: **no roadmap, no horizons, no priority framework** (the user is the PM), and ship cadence moved *onto the lead* because the lead is the only seat holding both the intent and the codebase seams. That same rationale keeps it off a standalone skill: it needs the `general` map and the grill in one head, which a standalone skill would have to rebuild cold. It's named `brief` for the file it writes, and to stay clear of opencode's own plan mode. A bare `lead <task>` still grills by judgment — it just doesn't persist. Hands the brief to `planner`; its other half is `TODOS.md` capture.
- **`todo`** / **`issue`** (`skill/kru-todo/`, `skill/kru-issue/`) — **self-contained user-invoked capture skills**, promoted out of the lead's verb list: `/kru/todo <the thing>` logs something the user wants but doesn't want done now into the store's `TODOS.md` as a line carrying `value` and `effort` (`1`–`5`, `~n` when the number is the session's proposal rather than the user's — the pair `todos` ranks on, and the `plan` field now holds the effort slug `effort` used to), and `/kru/issue <what's wrong>` opens `issues/<slug>.md` from what they just said, with every field it didn't investigate written as `_not investigated_` **in the file** so a cheap stub can never read as a finished write-up. Both return straight to whatever was in flight. They're the user's front doors to the two artifacts Step 4.5 already sweeps into, for when no task is running to sweep from; `brief` reads both back during the next grill and the user (the PM) decides what gets pulled into scope. The store's three-way split routes them: **wrong → `issues/`, wanted → `TODOS.md`, unformed → `notes/`**.
  **`todo` expands from loaded context, never from a fetch**. Logged mid-session — the file was just read, the error just seen, the decision just made — the line carries up to two sub-bullets of what the writer already knew (`file:line`, the constraint, why it's deferred). Logged cold, it's the user's sentence and nothing else. The rule is **cost, not usefulness**: already-loaded context is free and evaporates if unwritten, but no `general`/grep/file-open is ever justified to enrich a line — that's the task the user just declined. Expansion adds detail, never commitment: past the two numbers, no triage, no scheduling, no scoping into the current effort — and the numbers obey the same rule, taken from the user's own words, or estimated and written `~n` — a proposal the confirmation hands back, never fetched for. The rule is shaped around the common case — a *deferred want* raised mid-build, where the session already holds exactly the context that makes the line worth re-reading later.
  **Both are `disable-model-invocation`, and that's the design.** A user-invoked skill can't be reached by the agent *or by another skill* (`writing-for-agents` → `SKILL-MECHANICS.md`), so the lead can't fire them — which costs nothing, because the lead's own capture channel is the Step 4.5 sweep, written with the code already in context. What it buys is the thing that made the split worth doing: **zero standing context load**, where three model-invoked descriptions would have been permanently loaded in every session to save lines that only load when `lead` runs. It also enforces the no-autonomous-triage rule structurally rather than in prose — an agent that *can't* fire `/idea` can't autonomously triage a thought into work nobody asked for.
  **`todos` / `issues` are the working halves** (`skill/kru-todos/`, `skill/kru-issues/`) — same four steps each: **reconcile** every entry against the code (a want is done · archived · open; a defect is fixed · moved · open), **score** what's unsized (`value`/`effort` on a want, `effort` beside the filed `severity` on a defect — `~n` until the user confirms), **propose** a batch of `effort: 1`–`2` entries across different seams off the ranking (`value ÷ effort`; severity then effort; ties newest first), and **gate** — one message, then wait. Nothing is written before the user answers: confirmed-done lines and confirmed-fixed files are deleted, confirmed digits go bare, and each accepted entry reaches exactly one named outcome — *built/fixed* (one commit each, its entry deleted at Step 4.5), *already done* (deleted, nothing built), or *bigger than it says* (rescored in place, parked or handed to `brief`, never half-built). That gate is the whole guardrail: the order is arithmetic, the batch is a proposal, what gets built is the user's — the same call `brief` hands them. This is the exit `TODOS.md` never had (`issues/` was always emptied by fixes) and the only place a filed number meets the codebase. Singular writes one, plural works them — one word per artifact. `disable-model-invocation` for the write halves' reason plus one of their own: an agent that can fire `/kru/issues` can pull the defect list into a task nobody pointed it at.
- **`landed`** / **`comment-fix`** (`skill/kru-landed/`, `skill/kru-comment-fix/`) — **self-contained user-invoked change-hygiene skills**, the bookends around a landing. `/kru/landed` closes a change out: it reads `state: MERGED` off the PR rather than asking git, because a squash or rebase merge leaves none of the branch's SHAs in the base — `git branch -d` refuses it and `git log base..branch` still lists every commit, so the `-D` people reach for is being made blind. It guards on a dirty tree, on local commits past the PR's `headRefOid`, and on a base that needs more than a fast-forward, then syncs and prunes. `/kru/comment-fix` audits one target (working tree · path · branch · PR) against the standard the user's own `AGENTS.md` sets — lowercase, for the next reader of the code, never for the prompter — and carries that standard itself, since a plugin lands in repos whose instructions don't state it. Its bound is the target: a *diff* target audits what the change wrote, not every comment in the files it grazed, and its completion criterion is checkable — every hunk in the resulting diff is a comment line. Both are `disable-model-invocation`, same reasoning as `todo`/`issue`: a branch delete and an unasked-for restyle are exactly the two things the agent should not be able to start on its own.
- **`doc-fix`** (`skill/kru-doc-fix/`) — **self-contained user-invoked change-hygiene skill**, `comment-fix`'s sibling for human doc prose: `/kru/doc-fix` audits a target's `README`/`CONTRIBUTING`/`DEPLOY`/`docs/` against the house standard — keep the traps, the consequences, and what no run can teach; point at the canonical file for everything else — cutting negatives, run-output narration, code restatement, and era-bound status. Agent-consumed documents (`AGENTS.md`, `AGENTS.md`, skill/agent definitions) stay `writing-for-agents`' lane. Same checkable completion criterion: every hunk in the resulting diff is doc prose. `disable-model-invocation` for the same reason as `comment-fix`: an unasked-for doc restyle is exactly what the agent should not start on its own.
- **`prose-fix`** (`skill/kru-prose-fix/`) — **self-contained user-invoked change-hygiene skill**, the third `-fix` verb: `/kru/prose-fix` audits the strings a target renders to the user — JSX text, `placeholder`/`aria-label`/`alt`, i18n source-locale entries, form-action errors, toasts, email templates — against a standard it *points at* rather than restates: `ui-patterns`' text-and-icons group and `ux-copy`. It cuts UI narration (the line describing what the screen already demonstrates), outcome claims the code can't hold, a stored name at a sentence's head, vague actions, a field error restating its label, and the AI tells and em-dashes `doc-fix` bans, with the same binary grep check. Two things distinguish it from its siblings: a cut narration line is, per the `ui-patterns` rule, a defect report on the control, and the control is code — so the pass cuts the line and names the control's `file:line` in the report; and a string has a shadow — the test literal that pins it — which gets the same edit, the one non-UI file the pass moves. Log lines, developer `Error` messages, fixtures, and marketing routes (`copy-editing`'s) stay out. `disable-model-invocation` for the same reason as `doc-fix`: an unasked-for copy restyle is exactly what the agent should not start on its own.
- **`update`** (`skill/kru-update/`) — **self-contained user-invoked maintenance skill**: `/kru/update [claude|vendored|libs]` sweeps the three grounds that move under the plugin — the opencode releases above the mark (`claude.md`), each vendored copy's provenance sha against upstream's head (`vendored.md`), each first-party skill's `Reproduced on` pin against the registry and the release notes for the range (`libs.md`) — and returns a verdict per change with the file it lands in. **The thing on disk is the authority and the announcement is the lead**: a candidate frontmatter key is confirmed against the zod schema in the binary (`claude plugin validate` passes on any unknown agent key, so it answers a different question), a subcommand against its `--help`, an upstream commit against its diff, a release note against the identifiers the skill claims. It writes `reviewed.md` and nothing else — one section per ground, declines recorded with their reason so the next sweep doesn't re-litigate them; wiring an adoption is `/kru/roster`'s, a re-sync or re-verification is `/kru/roster author <name>` → *Refresh*. `disable-model-invocation` for the same reason as `todo`/`issue`: a sweep the agent can fire is a network fetch and a prompt-text rewrite nobody asked for.
- **`setup`** (`skill/kru-setup/`) — **self-contained user-invoked bootstrap skill**, and the plugin's other direction: `/kru/setup` derives a repo's standing answers once — seat pointers, the **conditional skills** this repo answers *yes* to (the library-choice check a seat re-runs on every dispatch, and skips silently), the seats this repo has no footing for, and whatever the repo's own docs leave unwritten (the gate that closes its token file, **what a test run costs**) — and writes them into that repo's `AGENTS.md` in that file's own idiom (`sheet.md` carries the fields and two renderings). What lands is **deliberately thin — a pointer at `kru-lead` plus the sheet, ~400 tokens** — on the split that decides everything here: *how the team works* is needed only once work starts, so it loads on demand from the plugin, while *who is on the team here* is what tells a session there is a team at all, so it is the only part that goes ambient. The pointer only fires because `lead` keeps a `description`; a user-invoked skill is unreachable by the agent and by other skills, which would make that line decorative. The sheet loads on every session in that repo with nothing typed, so routine work runs off the answers and `lead` loads only for what a sheet can't hold (greenfield, `planner`, a stack with no seat). Two rules carry it: **every derived line cites what it came from**, so a re-run can tell a stale line from a live one, and **the repo's own `AGENTS.md`/`AGENTS.md` is a source of truth** the block references rather than restates. The citations are what make a re-run an edit rather than a second copy — a derived line can be told from the repo's own — and one HTML-comment stamp is what makes the sheet a cache instead of a fork; re-running is the only update path, since the sheet is a function of *(plugin version × repo state)*, so re-deriving settles a repo that moved and a plugin that moved alike. It carries a **second job**: loading `writing-for-agents` and tuning the file it lands in — seven bars run over the line it adds and the line already there alike, cutting the no-ops, duplication, cheap caches, self-description, within-section restatement, prose a gate already enforces, and stale lines an always-loaded file accretes; each cut is proposed with the file that already answers it, because a good section appended to a sedimented file does not make that file good. **`/kru/setup prose` runs that job alone** — the surface read, the bars run, the cuts proposed, no derivation and no team section — the sibling of `/kru/comment-fix` (comment lines), `/kru/doc-fix` (human prose) and `/kru/prose-fix` (rendered strings) for the always-loaded agent surface, kept in the skill that owns the bars. It writes to a **checked-in** file, so it shows the whole diff and waits. `disable-model-invocation` for the same reason as `todo`/`issue`: editing a repo's committed instructions is not something the agent should start on its own. Its counterpart upstream is `/kru/roster learn`, which routes a one-engagement preference to a repo's sheet and a two-engagement one into the plugin (`PREFERENCES.md`).
- **`modern-css`** (`skill/kru-modern-css/` — Baseline judgment for which native CSS is safe to reach for now: the three Baseline states, an aging rule that keeps a hardcoded year from going stale, and an old-technique → native-CSS replacement table; exhaustive Baseline 2023–2025 feature list disclosed to `reference/baseline.md`). Loaded by the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`, `web-components-builder`) — **ambient**, same rationale as `typescript`/`ark-ui`: the builder checks Baseline safety in-place before reaching for a JS workaround or an older hack.
- **`modern-html`** (`skill/kru-modern-html/` — the HTML half of the same judgment: which native elements now do a job that used to need a library, and which are safe today. `<dialog>`/`showModal()` vs the `popover` attribute, invoker commands, `<details name>`, `inert`, constraint validation and `:user-invalid`, `<output>`, `<search>`, the loading/priority hints; the grouped status table disclosed to `reference/baseline.md`). Loaded by the **UI component builders** — **ambient**, same rationale as `modern-css`, which it defers to for the three Baseline states and the aging rule rather than restating them. **Deliberately not a semantics primer** — landmarks, heading order and `<button>` over `<div onclick>` are things every model already writes correctly, so the skill would fail its own entry bar by including them; what earns the load is the newly-native interactive layer and its traps (`<dialog open>` is not modal; a popover is not a dialog; `:invalid` matches a field nobody has touched). It also owns the **native-first gate** that runs before `ark-ui` — written once, in `ark-ui`'s reach-for section, so the two can't drift. Every status verified against `api.webstatus.dev` rather than recalled: the draft's remembered Baseline years were wrong on Popover (2024 → **Newly, 2025-01-27**) and stale on `:user-invalid` and `<search>`, both of which crossed to **Widely** in 2026.
- **`web-components`** (`skill/kru-web-components/` — vanilla custom elements and shadow DOM: the constructor's restrictions and the property that shadows its own accessor before upgrade, `connectedCallback` re-firing on every move, `innerHTML` re-renders eating focus mid-typing, what crosses the shadow boundary in each direction, `CustomEvent` defaults that trap an event inside the root, `ElementInternals` form participation, one `AbortController` per element; two disclosures — the grouped status table to `reference/baseline.md` and the third-party-embed half to `reference/embedding.md`). **`web-components-builder`'s playbook**, the same not-ambient posture as `sqlite` — a separate file rather than prose in the seat so the recipes stay the single source for anyone reading them directly. Authored because **almost nothing it covers throws**: the element upgrades, the console is clean, and a keystroke is dropped or an event reaches nobody. The load-bearing constraints are platform status, not opinion — **scoped custom element registries** and **reference target** are both Limited, so a tag name is a global with no escape and ARIA still cannot cross a shadow root; `:host-context()` and customized built-ins (`is=`) are Limited too, while `adoptedStyleSheets` and form-associated elements have been **Widely available since 2023-03**. `reference/embedding.md` carries what only bites in someone else's page: the registration guard, the host's CSP (constructed stylesheets are *specified* to need `'unsafe-eval'` but **no browser blocks them**, per MDN — an implemented-behavior fact, not a design guarantee), `position: fixed` breaking under their transformed ancestors, third-party cookies, and the theming contract as a versioned public API. It also records the one **open architectural question** rather than answering it: Stripe's docs are silent on mounting Elements inside a shadow root, and the single known report (`stripe/stripe-js` #143, 2021, closed with no documented resolution) is too old to be a verdict — so the skill says *spike it before the architecture depends on it*.
- **`ui-patterns`** (`skill/kru-ui-patterns/` — the team's house rules for how a component *behaves*, indexed by *what you're about to build*; four groups in `reference/`: `forms-and-mutations` · `lists-and-rows` · `text-and-icons` · `terminal-output`). Loaded by the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`, `web-components-builder`) at build time — **ambient**, same rationale as `typescript`/`ark-ui`/`modern-css` — by the **three framework builders** for `forms-and-mutations` alone (they carry the stack mechanism; the rule lives here), and by **`ux-designer`** when it specs a component, so a handoff spec and the build it's handed to can't contradict each other. `terminal-output` reaches the seats that ship a binary — `go-fullstack-builder` and `python-developer` — on the same one-group-per-slice terms. This is the **destination the preference sweep writes to**: `/kru/roster learn` promoting a design-lane preference files a corpus entry instead of editing a seat prompt, which is what keeps six seats from each carrying a copy of the same rule. Its entry bars are the whole design and they live in `CURATION.md` (authoring-only, unreferenced from `SKILL.md`, so it costs nothing at build time). **Every entry names the default it corrects**, and a pattern a careful builder would produce anyway doesn't go in — the corpus grows with the number of *wrong defaults*, not with the number of preferences the user has, which is the bound a flat pattern list never had. And **every entry is design-system agnostic** — behavior, semantics and grouping that hold under any token file: validation timing, where a failed submit sends focus, where an outcome reports, what a repeated control announces, how an icon boxes itself against a wrapping label. Rank, ink, borders, underlines, indicators — the token file settles those and settles them per project, so a rule about them here would be overridden in a repo with a real system and, worse, would contradict it while both read as authoritative. The test runs on the whole entry rather than a core carved out of one: focus-versus-selection has a genuine behavioral residue and still stays out, because every mark it turns on belongs to the system. Indexed by build target rather than by rule category for the same reason `ux-principles` groups by flow step: a builder loads one or two files per slice, never the corpus. Values stay the project's token file's, Baseline status stays `modern-css`'s, primitive anatomy stays `ark-ui`'s, and whether the *journey* works stays `ux-principles`'/`ux-auditor`'s.
- **`panda-css`** (`skill/kru-panda-css/` — the build-time extractor's blind spots, the two recipe forms and which one survives a runtime variant, `cx` vs `css()` merging, `include`/artifact/layer mechanics). Loaded by the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`) **only when the repo uses Panda** — **conditional, not ambient**, the same posture as `drizzle`: a styling system is a repo fact, and the brownfield rule forbids introducing one. Authored because every failure it covers is **silent** — Panda reads source *text*, so an unresolvable value emits no CSS while `panda cssgen` still prints success. The load-bearing findings, all reproduced on `@pandacss/dev@1.12.0`: a **config recipe** driven by a runtime variant returns a class with no rule in the sheet (`cva` emits every variant, a config recipe only the literally-used ones — `staticCss` is the safelist), `cx(css(a), css(b))` conflicts are decided by **generated-sheet order** and flipped when an unrelated file that never imports the component was deleted, the default `include` globs silently skip Next's `app/` and every `.svelte`/`.vue`/`.astro` file, `strictTokens` is a **type-only** gate (`cssgen` happily emits `color: nosuchtoken`), and `_dark` compiles to `.dark` alone so a `data-theme` app flips nothing. Docs and npm agree on v1 here, so the skill carries no version split — just the `beta` 2.0 (Rust/Oxc rewrite) as an opt-in to check `package.json` for.
- **`zod`** (`skill/kru-zod/` — the Zod 4 calls that fail by returning a value: `.default()` skipping its own checks, `z.coerce.boolean("false") === true`, `safeParse` throwing on async schemas, `.catch()` that isn't try/catch, unchecked transform output, `flatten()` losing nested field identity; error plumbing and per-boundary parse recipes disclosed to `reference/`). Loaded by the five **network-boundary seats** (`sveltekit-builder`, `nextjs-builder`, `react-router-builder`, `tanstack-start-builder`, `cloudflare-builder`) **only when the repo has `zod`** — **conditional, not ambient**, same posture as `drizzle`/`panda-css`: a validation library is a repo fact, and the seat that owns the boundary owns the parse. Authored because every failure it carries is a **successful parse returning wrong data** — `z.string().min(5).default("ab")` yields `"ab"` with no complaint from `tsc` or the runtime, and `z.coerce.boolean()` on the string `"false"` yields `true`, which is every env-var flag. The skills.sh `zod` pack was **rejected** on the Drizzle-row precedent: 43 quickstart rules Context7 already serves current, tagged `zodVersion: "4.x"` while teaching Zod 3 spellings, and its error-handling rule claims `flatten()` produces `'profile.name'` keys — reproduced as false (the key is `profile`, so a nested form error can't be routed to its input). Everything in the skill verified against `zod@4.4.3` + TypeScript 7.0.2, including the measured `zod/mini` bundle delta (70,489 B → 10,432 B minified) that makes it a real lever in a Worker.
- **`valibot`** (`skill/kru-valibot/` — the Valibot 1.x calls that fail by never running or by returning a value: `v.is()`/`v.assert()` over an async schema returning `true` under strict TypeScript, `v.toBoolean("false") === true`, a failed `safeParse` with `output` populated, `fallback` turning a failed validation into a typed default, `optional` rejecting the `null` a JSON client sends, a `union` of objects reporting "Expected Object but received Object"; issue plumbing and per-boundary parse recipes disclosed to `reference/`). Loaded by the five **network-boundary seats** (`sveltekit-builder`, `nextjs-builder`, `react-router-builder`, `tanstack-start-builder`, `cloudflare-builder`) **only when the repo has `valibot`** — **conditional, not ambient**, same posture as `zod`: a validation library is a repo fact, and the seat that owns the boundary owns the parse. Authored because the type checker gates only half of it — `v.parse` over a `pipeAsync` is a compile error, `v.is` over the same schema compiles clean and returns `true` for every input — and because Valibot's own published agent skill (`valibot.dev/.well-known/agent-skills/valibot/SKILL.md`) recommends `v.pipe(v.string(), v.toBoolean())` for env flags, which reads `FEATURE=false` as on. That skill was **rejected** for vendoring on the Drizzle-row precedent: a Zod-vs-Valibot spelling table plus quickstart that `llms.txt` and the first-party MCP server already serve current; the skill points at it for the spellings. Everything verified against `valibot@1.4.2` + TypeScript 7.0.2 with `exactOptionalPropertyTypes`, including the measured bundle (3,014 B minified for a two-field schema, against 433,303 B for `zod@4.5.4` classic and 12,558 B for `zod/mini`) that is why a repo picked it.
- **`superforms`** (`skill/kru-superforms/` — Superforms 2 on SvelteKit: the absent field filled from the schema before validation, nested values discarded under the default `dataType`, a form id hashed from the schema shape, client proxies writing `NaN`/`1` for `"1,5"`, and `withFiles` being the same function as `removeFiles`; the action half and the `superForm` half disclosed to `reference/`). Loaded by **`sveltekit-builder` + `svelte-ui-builder`** **only when the repo has `sveltekit-superforms`** — **conditional, not ambient**, same posture as `zod`/`drizzle`/`panda-css`: a form library is a repo fact. Authored as the natural sibling of `zod` — that skill ends where the `ZodError` is handed over, this one starts there — and because every failure it carries reports **`valid: true` about data nobody entered**: a completely empty `FormData` against a four-field schema returns `valid: true, errors: {}` with `role: "admin"`, because a missing key is filled from its JSON-Schema type and an enum fills with its first member. `strict: true` fixes that and breaks every checkbox; the working pairing (`strict` + `.default(false)` per boolean) was reproduced, not assumed. **No skills.sh pack exists**, so nothing to evaluate or reject. Carries the team's one **seam exception**: Superforms 2.30.2 ships zero `.svelte.js` modules, so `$form` is a store and a field component must take the whole `SuperForm<T>` + a `FormPathLeaves<T>` path rather than deconstructed serializable props — stated in both seats so neither invents a different answer. Everything verified against `sveltekit-superforms@2.30.2`, with the `superForm` option defaults read out of the shipped `dist/` (the docs' table lists no defaults, and v1-era tutorials teach the opposite ones).
- **`conform`** (`skill/kru-conform/` — Conform on React: `""` stripped to `undefined` before validation so the required message you wrote never fires and `.default()` makes a cleared field a success, an absent array validating as `[]`, `z.instanceof(File)` passing an empty file input, an intent submission with no `status` and no `value`, and `reply()` echoing the submitted password into the HTML; the action half and the component half disclosed to `reference/`). Loaded by the three **React network-boundary seats** (`nextjs-builder`, `react-router-builder`, `tanstack-start-builder`) and **`react-ui-builder`** **only when the repo has `@conform-to/react`** — **conditional, not ambient**, same posture as `zod`/`superforms`/`drizzle`/`panda-css`: a form library is a repo fact, and the brownfield rule forbids introducing one. It carries the split the docs don't flag — a classic API and a preview `future` API in one package, with the integration guides already teaching the preview's hooks — so a snippet copied from a guide page lands in a classic codebase and doesn't compile. Every claim reproduced against `@conform-to/react@1.21.1`.
- **`react-hook-form`** (`skill/kru-react-hook-form/` — RHF 7 on React: `formState` as a Proxy that subscribes only to what render read, the DOM's string arriving where the schema expects a number, an empty multi-select submitting `false` rather than `[]`, `handleSubmit`'s unconditional `preventDefault()` cancelling a server action, and `setValue` writing a value the form still reads as pristine; the boundary half and the component half disclosed to `reference/`). Loaded by the three **React network-boundary seats** (`nextjs-builder`, `react-router-builder`, `tanstack-start-builder`) and **`react-ui-builder`** **only when the repo has `react-hook-form`** — **conditional, not ambient**, the same posture as `conform`/`zod`/`superforms`/`drizzle`/`panda-css`, and the same brownfield rule: a repo already on Conform stays on Conform. It is the sibling of `conform` across the same seam and it fails the opposite way — Conform's parse **deletes**, so a value goes missing; RHF **opts in**, so the value is there and wrong. The flagship is a submit button that can never enable: `disabled={!formState.isDirty || !formState.isValid}` short-circuits before the Proxy ever reads `isValid`, so the subscription is never made and no amount of valid typing re-renders the button. Reading `isValid` is also what makes it track under the default `mode: 'onSubmit'` — reproduced at **0** resolver runs unread against **3** read over mount plus two keystrokes, which is the cost the docs state the rule without. The other half the seats need is the server boundary: `handleSubmit` calls `preventDefault()` on every submit, so `action` + `onSubmit` validates and saves nothing, while `action` alone skips validation and hands the server a `FormData` missing every `Controller` field — one boundary, the action called inside the valid handler. Unlike `conform` there is **no API split to navigate**: `zodResolver` covers Zod 3 and Zod 4 from one export, detecting v4 by the schema's `_zod` property. Every claim reproduced against `react-hook-form@7.87.0` + `@hookform/resolvers@5.9.1` with `zod@4.5.4` and `react@19.2.8`.
- **`xstate`** (`skill/kru-xstate/` — XState 5's actor model: an invoked promise actor's rejection with no `onError` propagating up as an **uncaught exception on the process**, not a caught in-machine error; an `always` transition with no exit condition looping until the process **OOMs** (reproduced — the one trap here that can take a whole dev session down, not just the actor); `invoke` torn down and recreated from nothing every time its state is re-entered where `spawn` (held in `context`) survives across transitions; sequential `assign` actions resolving in order rather than against one shared snapshot; and the docs' own `llms.txt` indexing a `v6` tree whose quick start says `npm install xstate@alpha`, which breaks the peer range `@xstate/react@latest` still pins to `^5.28.0`; actor-communication/logic-creator/testing recipes disclosed to `reference/actor-communication.md`). Loaded by **`react-ui-builder`** **only when the repo has `xstate`** — **conditional, not ambient**, same posture as `conform`/`react-hook-form`/`zod`. Scoped to the one seat rather than shared with the React network-boundary seats the way `conform`/`react-hook-form` are: every reproduced trap here is about the actor runtime itself, not a network boundary, and the concrete integration this team has verified is component-level (wizards, players, multi-step flows) — a server-orchestrated use would need its own research before wiring those seats in. Authored because the actor model's failures are the opposite of loud: an actor error doesn't throw where it happened, it throws the *next time anything reads the actor* (its sibling `xstate-react` skill's crashed-actor-in-render section), and an eventless-loop bug produces no error at all until memory runs out. Every claim reproduced against `xstate@5.32.6` (npm `latest`) with `@xstate/react@6.1.0`, Node 24.
- **`xstate-react`** (`skill/kru-xstate-react/` — `@xstate/react` 6's hooks: `useMachine`'s `input` read once at the actor's creation and never again, so a prop that changes on a later render leaves context stuck on its first value with no error (reproduced, same shape as `react-hook-form`'s `defaultValues`-at-mount trap); a machine defined inline in the component body getting a new identity every render, which the hook detects via `logic.config !== currentConfig` and silently tears down and recreates the actor every render (invisible in the rendered state, real in actor churn); `useSelector`'s default `===` compare re-rendering on every emitted snapshot when the selector returns a new array/object (reproduced: 2 unrelated context updates → 2 extra renders unstable, 0 stable) with the package's own `shallowEqual` export as the fix; and a crashed actor's error surfacing by **throwing during a render pass**, which only an Error Boundary stops from blanking the tree — a `try`/`catch` around the `send` that triggered it does nothing, since the throw happens on a later, unrelated read. Loaded by **`react-ui-builder`** **only when the repo has `@xstate/react`** — same conditional posture and same single-seat scoping as `xstate`, its sibling and prerequisite (a repo can have `xstate` without `@xstate/react` — server-only orchestration — but not the reverse). Authored rather than left to the docs because every trap here is the same seam-crossing failure in a different shape: React re-renders on a schedule XState's actor doesn't know about, and the hook's job is bridging that gap, which is exactly where each of these five findings lives. Every claim reproduced against `@xstate/react@6.1.0` + `xstate@5.32.6` + `react@19.2.8` under jsdom + `react-dom/client`.
- **`ux-principles`** (`skill/kru-ux-principles/` — canonical UX principles from Krug, Norman, Nielsen, Yablonski and Cooper, each carrying the **code signal** that betrays it in source and the fix that resolves it; six groups disclosed to `reference/`, grouped by what the flow *step* is rather than by book, so a walk loads two or three files instead of six). Loaded by **`ux-auditor`** (its whole job) and **`ux-designer`** (its handoff specs enumerate the same interaction states) — **not ambient on the UI builders**, deliberately: this is audit apparatus, not construction reference, and paying it on every UI build buys little. Authored against `skill/kru-roster/author.md`'s entry bar, which a restatement of the canon would **fail** — every model already knows the ten heuristics, so the principles are not the contribution. What no source publishes is the **detection layer**: what Norman's signifiers look like in a `.tsx` file. Hence the rule that shapes the whole corpus — *if you cannot name a concrete, source-visible tell, the principle doesn't go in* (peak-end is true, load-bearing, and correctly absent). The **`Detect:` tier** is the other load-bearing part: `STATIC` is reportable as a defect, `HEURISTIC` only as a question, `RENDERED` not at all — because an auditor reading source has no pixels, and one fabricated finding costs more trust than ten real ones earn. Every entry carries a free, **verified-resolving** URL beside its book citation, since a book nobody can open is an unverifiable claim.
- **the 3D lane** (`skill/kru-webgpu-threejs-tsl/` vendored, + Context7 for core three / Threlte / R3F). Loaded by the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`) — **3D is ambient**, same rationale as `typescript`/`ark-ui`/`modern-css`: a `<Canvas>` is a component, so the seat that owns the component owns the scene inside it, one owner end-to-end. Built through the **framework wrapper** (Threlte on Svelte, R3F + drei on React), never vanilla imperative three, so the scene graph stays a component tree behind the team's usual serializable-props seam. The vendored skill is deliberately **narrow** — TSL/WebGPU/compute only; core three and the component layer are Context7-backed, because unlike motion/copywriting an official three.js source exists and outranks any pack. Non-interactive hero/ambient art (Veo video, p5.js generative) stays `graphic-designer`'s.
- **`drizzle`** (`skill/kru-drizzle/` — the ORM layer over both data seats: the version split between npm `latest` (0.45.x) and what orm.drizzle.team documents (v1 rc), reviewing the SQL `drizzle-kit generate` writes before it ships, `push` vs `generate`+`migrate`, and what Drizzle doesn't do for you; `postgres`/`sqlite`/`migrations`/`queries` disclosed to `reference/`). **Shared by two seats, not ambient** — `postgres-architect` and `sqlite-architect` each load it plus their dialect file when the repo uses Drizzle; it sits *over* the engine knowledge in the `sqlite` skill and the pg seat's own discipline, never restating either. Authored rather than vendored, and the skills.sh `drizzle-orm` pack **rejected**: it teaches the `@deprecated` object-form table config, hand-rolled `relations()` with no version caveat, and an unsourced perf table — generic quickstart material Context7 already serves current, which `SOURCES.md` forbids adding as a third-party pack. What justifies a repo-authored skill instead is the part no doc states: **drizzle-kit's SQLite table rebuild silently deletes `ON DELETE CASCADE` child rows** (reproduced: 3 rows → 0, migration reports success), the migrator takes no advisory lock and batches every pending migration into one transaction, and a migration merged with an older timestamp than the last applied one is skipped forever. Every claim verified against the shipped package, not release notes.
- **`tdd`** (vendored, `skill/kru-tdd/` — the red → green loop, **seams** as where tests go, the three anti-patterns (implementation-coupled, tautological, horizontal-slicing), good-vs-bad tests, mock-at-boundaries; from `mattpocock/skills`, kept verbatim). Loaded by the thirteen **behavior seats** — `sveltekit-builder`, `react-router-builder`, `nextjs-builder`, `tanstack-start-builder`, `go-fullstack-builder`, `cloudflare-builder`, `postgres-architect`, `sqlite-architect`, `better-auth-specialist`, `stripe-specialist`, `paypal-specialist`, `web-components-builder`, `python-developer` — alongside its repo-authored sibling **`testing`** (next entry) as **Block D** in `skill/kru-roster/shared-blocks.md`: **test-first is ambient**, same rationale as `typescript`/`ark-ui`. First coverage happens inside the build, where the seat already holds the contract, instead of in a second seat that has to rediscover the harness and re-read files it didn't write. Four exemptions, each recorded rather than assumed. Three say the seat *can't go red yet*: **visual/motion** (the UI builders can't go red on a layout — the user's visual-intent inspection is that gate), **unknown shape** (a spike has no interface to test yet), **no harness** (standing one up is `toolchain-engineer`'s). One is about **latency**, and only where the user is genuinely the oracle: **the slice's deliverable is a screen** — a property of the slice, not the seat; the `load`/action feeding it ships with it and is covered once the intent settles. It's a **deferral, not a waiver**: the lead names it in the handoff and owes the covering slice, and a builder never self-exempts on it. Bounded by its converse, which is the rule that keeps it from spreading — **where the eye can't tell, there is no exemption**: end-to-end seam code (route → data layer → render → action → write) is exactly what a glance can't verify, is the cheapest test in the project, and is the net every later slice hangs off, so "it's the first version" buys nothing (`lead` Step 3, Block D). **A bug fix has no exemption** — its repro test lands in the same change that deletes its `issues/` file (`lead` Step 4.5), because deleting the file makes the test the only surviving record of the defect. The skill is **vendored verbatim**, so the one adaptation it needs — its "confirm the seams under test with the user before writing any test" step (v1.2.0's replacement for the old behavior-list approval) is impossible inside a subagent — lives in the seats and in the lead's handoff (the grill *is* the approval, and the handoff names the seams), never as an edit to the vendored file.
- **`testing`** (`skill/kru-testing/` — discovering a repo's own testing conventions before writing a line, the principles of a test worth keeping, and the run→fix loop to green). Loaded by the thirteen **behavior seats** as the other half of **Block D**, and by `test-writer`, which now *runs* it rather than restating it — **testing craft is ambient**, same rationale as `typescript`/`ark-ui`. Extracted from the `test-writer` prompt to close a real hole: Block D made builders the authors of most first coverage while the discovery discipline and the principles still lived only in a seat they never load, so the seats writing the most tests had the least guidance. **`tdd` supplies the loop; `testing` supplies what makes each test in it good** — vendored and authored halves of one job, which is why neither restates the other. What stays seat-only is what needs its own context: fan-out across many files (a sweep inside a builder's context is what `test-writer`'s hygiene rules exist to prevent), repairing a suite when no builder is in flight to be ambient *to*, and writing captured conventions down. Earns authorship over a lookup (`/kru/roster author`) by carrying what no runner's docs will: the failure modes specific to an agent writing tests unattended — a watch-mode script that hangs the run and returns nothing, mock state leaking between tests, assertions that only prove the code ran, and reaching for a different runner's near-identical API from habit. Stated **runner-agnostically on purpose** — the seat's whole posture is deferring to the repo's harness, so naming one runner in a principle biases it against the repo it's reading.
- **`vitest`** (`skill/kru-vitest/` — the runner's own silences, each a run that reports what you did not ask: a `*.test-d.ts` the default `include` never collects under a green `vitest run`, a passing test's `console.log` that the agent-detected **`minimal` reporter** drops (Vitest switches to it when `CLAUDECODE`/`CURSOR_AGENT`/`CODEX_SANDBOX` is set), `.only` and a new snapshot that pass locally and fail the file in CI (the missing snapshot reported as **`mismatched`**), `mockReset: true` turning every module-scope `mockResolvedValue` into `undefined`, `advanceTimersByTime` missing a timer queued behind an `await`, and Testing Library's `waitFor`/`user.click` hanging to `testTimeout` under fake timers — where the documented `advanceTimers` option did not unblock it and `shouldAdvanceTime: true` did; the module-mocking half and the jsdom/Browser Mode half disclosed to `reference/` — Browser Mode reproduced under Chromium too: `expect.element` retrying to the 15 s test timeout while the docs name 1 s, and a cold run whose dependency scan one file breaks failing every other file until the second run). Loaded **only when the repo has `vitest`** — **conditional, not ambient**, the same posture as `zod`/`drizzle`, and carried by the **`testing` skill's discovery step**: `testing` is already the ambient half of Block D in every seat that writes a test and in `test-writer`, so the single pointer there is what puts the runner recipe under every test the team writes. It is the runner layer under `testing`, which stays runner-agnostic on purpose: `testing` says *one-shot, never watch*; `vitest` says the default is `!CI && stdin.isTTY` and what the run prints once it does exit. Earns authorship over a lookup on the same bar as the rest: the docs state every default exactly and are quiet about what you *see* — the reporter swap, the CI flips, the `mismatched` wording, the exit-1 run whose counts read `2 passed`. Everything reproduced against `vitest@5.0.0` with `vite@8.2.2`, `jsdom@30.0.1`, `@testing-library/react@16.3.3`, `@testing-library/user-event@14.6.7`, `react@19.2.8`; the v4 forks (`clearMocks` default, hoisting that warns instead of throwing, unawaited assertions that warn) marked in a table at the top so a 4.x repo reads the right row.
- **`testing-library`** (`skill/kru-testing-library/` — the query that finds the wrong thing and the interaction that does nothing, in a test that passes: `getByText` failing on text split across elements under every matcher form but the function, a string `name` matching the whole accessible name where the docs' regex habit matches every button containing the word, an `aria-label` that replaces the content and a placeholder that names nothing, the input types with no role at all (`password`, `date`, `file`), `getByRole` costing ~1 s per call on a 3,000-node DOM, a Tailwind `hidden` class that `getByRole` and `toBeVisible` both see through because Vitest doesn't process CSS, `toBeDisabled` passing over `aria-disabled` while `user.click` on it fires, `waitFor` resolving on a falsy return and re-running a click per poll, user-event 14 silently dropping the v13 descriptors (`{selectall}`, `{space}`, `{esc}`), and `renderHook`'s destructured `result.current` going stale). Loaded **only when the repo has `@testing-library/react` (or `/svelte`)** — **conditional, not ambient**, carried by the **`testing` skill's discovery step** exactly as `vitest` is: the runner skill owns the wiring (cleanup, the setup file, fake timers, the reporter) and this one owns what the queries and interactions do once wired. Authored on the same bar: the docs state every default — `exact: true`, the 1000 ms `findBy` timeout, the query priority — and are quiet about what the run *does* with them: that `exact: false` still can't cross an element boundary, that RTL's act-environment flag is set in a `beforeAll` Vitest never registers under `globals: false` so React 19's act warning has nowhere to print (and the agent-detected reporter drops it anyway), that jest-dom's README `types` entry leaves the matchers untyped under Vitest. Everything reproduced against `@testing-library/react@16.3.3` with `@testing-library/dom@10.4.1`, `@testing-library/user-event@14.6.7`, `@testing-library/jest-dom@7.0.1`, `react@19.2.8`, `vitest@5.0.0`, `jsdom@30.0.1`.
- **`python`** (`skill/kru-python/` — the Python failures that pass `ruff` and `mypy` and ship quiet: a lint rule that skips every `_`-prefixed name so a dead duplicate `def` survives, `x or default` eating a legitimate `0`, an `except` that returns a plausible value, a subprocess wrapper reporting a successful run as `"Error: "`, a `set` serialized in a different order every process, a package that shadows the module beside it; the MCP-server / packaging / test-mechanics halves disclosed to `reference/`). **`python-developer`'s playbook**, the same not-ambient posture as `go`/`sqlite`/`web-components` — a separate file rather than prose in the seat, so the recipes stay the single source for anyone reading them directly. **Harvested from `wdm0006/python-skills`** (MIT, sha bf544c1) but **not vendored**: the pack is 16 skills / ~7.9k lines whose front halves restate `pyproject`, `pytest` and `ruff` docs that Context7 serves current, and vendoring it would put a stale copy above the source — the same call the `sqlite` row made against `sqlite-ops` and the 3D row against `cloudai-x/threejs-skills`. What survived is the layer no source states, and verifying it corrected the pack four times: its `from mcp.server.fastmcp import FastMCP` is **1.x-only** and raises `ModuleNotFoundError` on a fresh install (`mcp` 2.x renamed `FastMCP` → `MCPServer`), its defensive `asyncio.to_thread` wrapper is **redundant on 2.x** (sync tools already run on an AnyIO worker thread) and load-bearing only on 1.x, its `-> dict` return annotation **silently drops structured output** on 2.x, and its return-an-error-dict convention **raises** from a tool annotated `-> int`. Every load-bearing claim reproduced on Python 3.14.3 / ruff 0.16.5 / mcp 2.1.1 + 1.29.1 / hatchling (the F811 skip and its config fix, the shadowed module, `reset_mock` keeping its stub, cross-process `set` order, `str.split` vs `shlex.split`, the non-zero exit that wrote to stdout, the green `uv build` shipping a wheel with none of the package in it and `twine check` passing anyway), and the MCP version table read off the installed SDKs' own source.
- **`sqlite`** (`skill/kru-sqlite/` — the connection pragma recipe and the cargo-cult pragmas to leave out, single-writer concurrency and what actually fixes `SQLITE_BUSY`, STRICT tables, the 12-step rebuild; drivers / migrations / ops disclosed to `reference/`). **Not ambient** — unlike `typescript`/`ark-ui`/`modern-css`, this one is a single seat's playbook: `sqlite-architect` loads it, and it's a separate file rather than prose in the agent so the recipes stay the single source of truth for anyone who reads them directly. Authored rather than vendored: the only SQLite skill on skills.sh is Turso's `turso-db` (libSQL, a different concurrency and pragma story), and the community `sqlite-ops` pack is Python-only and wrong on the two questions that matter (it prescribes WAL as the fix for `database is locked` and never mentions `busy_timeout`). Every load-bearing claim is checked against sqlite.org, not blog posts.
- **`go`** (`skill/kru-go/` — the Go traps that compile, pass `vet`, and ship quiet: a nil slice marshaling as JSON `null`, `http.Error` not ending the handler, a map race that is `fatal error` and skips every `recover`, a goroutine taking `r.Context()` and dying at flush (`context.WithoutCancel` is the fix, not `Background`), `omitempty` keeping a zero `time.Time` while dropping `false` (`omitzero`, 1.24+), `==` on a round-tripped time, the typed-nil `error`; security / `database/sql` / the test gate / the `go.mod`-directive version table disclosed to `reference/`). **`go-fullstack-builder`'s playbook**, the same not-ambient posture as `sqlite`/`web-components` — a separate file rather than prose in the seat so the recipes stay the single source for anyone reading them directly, and the seat's own trap block shrank to what only its *shape* decides (router, SPA serving, auth in-seat, the boundary per handler). Mined from `samber/cc-skills-golang`'s 14 ⭐-recommended skills (human-reviewed, MIT) but **not vendored**: at ~2.6 MB the pack is mostly lookups Context7 and `go doc` serve current, every `SKILL.md` carries a fan-out-subagents orchestration block that contradicts the no-self-dispatch rule, and its recommended set overlaps ~50% with golangci-lint by its own README. What survived is the layer no source states, and it corrected the seat five times — the blanket "every goroutine takes `r.Context()`" was killing background writes, `omitempty` was named as the mirror of the zero-value trap when it isn't one, `errors.As` is superseded by `errors.AsType[T]` on 1.26, `%w` was unqualified at the public boundary, and "no interface with one implementation" needed the seam carve-out a service test depends on. Every load-bearing claim reproduced on go1.26.1 (the double-write, the fatal race, the `null` slice, the `float64` number, `Trim` mangling `Bearer`), and every API named checked with `go doc` (`CrossOriginProtection`, `FileServerFS`, `OpenRoot`, `sql.Null`, `MaxHeaderBytes`, `NewMultiHandler`).
- **`tui-design`** (`skill/kru-tui-design/` — the terminal as a surface you share and don't own: the frame measured in **display cells** rather than characters (wide runes, zero-width marks, ANSI, and `go-runewidth`'s locale-driven ambiguous-width flip that shreds the same binary's frame in a `zh_CN.UTF-8` shell), color on a background and profile you have to detect, the render loop that must never await, the exit path that restores raw mode and the altscreen in every branch including panic, and resize as an event rather than a startup fact; the four stacks, robustness, and visual craft disclosed to `reference/`). Carried by `python-developer` (Textual) and `go-fullstack-builder` (Bubble Tea) beside their existing terminal-output pointer — a binary that *takes over* the screen is a different medium from one that prints to it. A Ratatui or Ink repo reaches it through the conditional row with no seat behind it (*Handling gaps*), which is the honest state: Rust has no seat, and `react-ui-builder`'s lane is the browser. Drawn from `pageton/tui-design-skill` but **not vendored, twice over**: the repo ships **no license**, and its framework pages are quickstarts Context7 serves more currently. Written first-party from the earned half, and the verification corrected the source four times — it hand-rolls a `recover` + `\x1b[?1049l` that fights Bubble Tea's default panic catching, it predates `ratatui::init()`'s own panic hook **and** that hook's ordering constraint (install `color-eyre` *before* `init()`, or the report prints into a raw-mode altscreen nobody sees), it teaches Lip Gloss v1 `AdaptiveColor` that v2 moved to `compat` in favor of `HasDarkBackground` + `LightDark`, and it omits the Charm v2 import-path split that makes pkg.go.dev's default page describe the wrong API. Claims checked 2026-09-11 against docs.rs, pkg.go.dev, textual.textualize.io and Context7; companion-library sets and versions off crates.io, PyPI, npm and the Go module proxy.
- **`design-gallery`** (`skill/kru-design-gallery/` — the in-repo component gallery: the preview format, standing one up, and the three things a screen gives a component for free that a preview supplies itself). **Convention over configuration** is the whole design — one file per preview, found by a glob, so **registration is the file existing** and a component can't ship missing from the surface people open to see what exists. It is ordinary app code at a dev-only route, covered by the repo's own typecheck and lint, which is what makes it a builder's to maintain. **User-invoked** (`disable-model-invocation`): a gallery is for human eyes, so nothing gains by firing it autonomously and it costs no standing context; `{KRU_HOME}/kru/references/ui-practice.md` carries the one pointer a build reaches it through. Two boundaries it shares with nothing: the **sync lane** (`{KRU_HOME}/kru/references/design-sync.md`) is a build product that publishes to people who don't write code, and **`/kru/visual-review`** sweeps rendered routes in a running app — a gallery renders components out of route context, so neither stands in for the other.
- **`design-system`** (`skill/kru-design-system/` — the seven foundations: color · typography · size · elevation · motion · primitives · shells, with `create` and `audit` disclosed as sibling branch files). It exists because a token file is **derived from its consumers**, so it is complete by construction and blind by construction: every hole in it is invisible from inside it. The rubric is the part no repo can supply — a foundation is answered by a closed **set**, a **rule** for picking a step, and a **consumer** spending it — and **the rule is the half that goes missing**: a ramp with no stated step semantics gets spent by eye and drifts inside its own token file while auditing green, which is why `--gray-6` loses to *the step a border takes at rest*. **Ladders before parts**, because a primitive audited before its ladders exist can only be judged on taste, and taste is the design's. Four decisions worth the record. **Shape, never value**: an influence (`--palette radix`) is taken for its step count and step semantics and its hues handed back, so the skill can run before the canvas without taking the canvas's decision. **A TS source emitting the token file** was chosen over CSS-with-comments because a rule nothing reads drifts by default — the rule becomes *data*, and the cost is stated rather than absorbed: the token file turns into a build product, so the check script that re-emits and diffs it ships in the same slice or the source of truth is a suggestion. **Elevation is its own ladder** rather than a clause inside shells: a shadow scale with no rule is the same failure as a colour ramp with none, and a level bundles border width, fill step and shadow the way a type step bundles size, weight and line-height — so shells point at the ladder instead of restating it. **Model-invoked**, unlike `design-gallery` — `ui-designer` must reach it on its coverage read, and a user-invoked skill can be fired by neither the agent nor another skill. `audit` stands alone on a repo that never ran `create`, reading a hand-written stylesheet or a `@theme` block, and where no rule is stated it reports the gap — writing an inferred one in would promote a guess to the authority every future value resolves against. Four findings, no fifth (no set · no rule · off-ladder · orphan), ordered by what they block; a palette you'd have chosen differently is not one, and contrast is recorded rather than computed. Values and the look stay the design draft's, consumer conformance stays the repo's gate's, what renders stays `/kru/design-gallery`'s and `/kru/visual-review`'s.

Skills: `/grilling` (stress-test the brief before planning — the engine under `/kru/brief`), `kru-artboards` (**first-party to this fork** — the artboard mechanics behind `kru/ui-designer`, standing in for claude's bundled canvas surface, which opencode cannot reach; the lead's design surface in every repo, on any stack), `kru/code-reviewer`, `kru-tdd`, `kru-diagnosing-bugs`, `/verify`, `/run`, `writing-for-agents` (authoring standard for this repo, model-invoked — see Growing the team), `/kru/roster` (**roster-ops**: `hire`/`retire` a seat, `author` a skill, `learn` (sweep learned preferences back into the team), `audit` for drift — the Growing-the-team checklist as a tool; runs in the main thread. This is team **self-authoring/governance**, not the excluded ops/HR company function — it mints the team's own members, it doesn't add a business function).
Evolution loop (`PREFERENCES.md`): `/kru/remember` (user-invoked — bank a liked pattern/preference) + agent-journaled learnings + `dispatch-auditor`'s hook-fired process findings write to a user-global inbox (`~/.claude/kru/`); `/kru/roster learn` sweeps it (gated) straight into the team — targeted `agents/<seat>.md` prompt edits, or the owning skill (`lead` SKILL.md for orchestration-wide rules). No central style file: plugins can't ship auto-loaded context, so a promoted preference lives where the seat already reads. Cross-project (belongs to the team, not any repo) — the sibling of the project-scoped `TRACKER.md` `TODOS.md`.
Vendored official skills: `react-router` (backs `react-router-builder`); `to-spec`/`to-tickets`/`wayfinder` (back `planner`); `accessibility-review` (WCAG audit, a11y sibling of the `/kru/visual-review` pass; it has a seat — `accessibility-reviewer` — so it answers to both a lead and a user caller); `user-research`/`research-synthesis`/`ux-copy` (back `ux-designer`; team-adapted — connector hooks remapped, and the tier discipline they shipped without added: reference tokens by name, never invent a measurement, and never report a contrast finding — colour is the design's and no pass computes a ratio) — from the knowledge-work plugins, vendored so their MCP fleets stay out (those packs' Product-Management skills are **not** vendored — the user is the PM, so nothing on this team would route them); `copywriting`/`copy-editing`/`cro` (**user-invoked copy passes**, run inline in the main thread — `/copywriting` writes/rewrites a marketing page's copy, `/copy-editing` line-edits it, `/cro` audits an underperforming page/form; builders ship working draft text and the user pulls the pass they want, copy being cheap to replace post-build; from `coreyhaines31/marketingskills` — a curated community pack, vendored since no first-party copywriting source exists); `find-animation-opportunities`/`emil-design-eng`/`animation-vocabulary`/`review-animations`/`improve-animations` (motion craft carried **ambiently by the UI component builders** `react-ui-builder`/`svelte-ui-builder` — the builder implements motion in-place from the plan's motion note/dial, same ambient pattern as `ark-ui`/`modern-css`; motion is checked in the user's visual-intent inspection, with `/kru/review-animations`/`/kru/improve-animations` available as user-invoked audits; from `emilkowalski/skills`, MIT — an authored pack from the reference web-animation source, vendored since no first-party animation source exists); `turborepo` (backs `toolchain-engineer`, from `vercel/turborepo`, MIT); `cloudflare`+`wrangler` (back `cloudflare-builder`, from `cloudflare/skills`, Apache-2.0); `webgpu-threejs-tsl` (the 3D lane's shader/compute half, carried **ambiently by the UI component builders** — from `dgreenheck/webgpu-claude-skill`, MIT per README; scoped narrow because three.js *does* have a first-party source, so Context7 outranks it on core three/Threlte/R3F) — all first-party except the marketing, animation, and TSL packs. Plus the reuse/maintenance skills now vendored for web self-containment: `codebase-design`/`tdd`/`diagnosing-bugs`/`domain-modeling`/`grilling`/`writing-for-agents` (from `mattpocock/skills`), and the locally-authored `local-browser` (wraps the first-party `chrome-devtools` MCP server, which drives its own headless instance and leaves the user's browser alone). See `SOURCES.md` → Vendored resources. Optional official subagent: `svelte:svelte-file-editor`.
Plan store: `TRACKER.md` (user-level markdown under `~/.claude/kru/management/<project-slug>/`, slug = repo dir name — same convention as the preference inbox) — `/kru/brief`'s + planner's plan-of-record store; replaces `/setup-matt-pocock-skills` and GitHub Issues. A working repo carries **no** management files and **no pointer** to the store — the slug convention is how a session finds it. Four lifetimes, **no roadmap layer**: `plan/<effort>/` (the current change — `brief.md`, `spec.md`, `tickets/`), a flat `TODOS.md` at the root that outlives every brief rewrite, `issues/` — one file per known defect, deleted by the change that fixes it — and `notes/`, freeform brainstorming with no lifecycle and no sweep, **input never authority**. The split: *wrong* → `issues/`, merely *wanted* → `TODOS.md`, *unformed* → `notes/`. The **lead reconciles it at commit** (`lead` skill, Step 4.5): flip done tickets, tick the brief's landed PR + `Done when` boxes, capture pitched/discovered wants to `TODOS.md`, and file/delete `issues/` — reconciled as each slice lands, no drift.

## Planned (add as the team matures)
- `astro-builder` — content-heavy sites. Backing: Context7 (`astro`). Mint when Astro work recurs.

## Growing the team
Adding a specialist is a versioned change — run **`/kru/roster hire <name>`**, which executes this checklist against its wiring map so no point is missed:
1. Write `agents/<name>.md` against the **`writing-for-agents`** standard (context pointers, the two loads, information hierarchy, single source of truth, aggressive pruning; `SKILL-MECHANICS.md` for the skill-only half) — single responsibility, official-source-first, quality floor, **present tense with no archeology**. Copy the shape of an existing specialist for the seat-specific half and **`skill/kru-roster/shared-blocks.md` for the shared blocks**, never a peer for those.
2. Wire it to its official source: add a row to `SOURCES.md`.
3. Add a row here in **Current specialists** + a **Model tiers** entry, and a routing row to the `lead` skill.
4. Recompute the agent count (`ls agents/*.md | wc -l`) into `plugin.json` + `marketplace.json`; bump `VERSION`, `plugin.json` `version`, and the header above (minor bump for a new agent).
5. `git add -A && git commit && git tag vX.Y.Z`.

Adding a **skill** is the smaller change (`/kru/roster author`) and carries its own entry bar: a skill is **recipes earned by working with the thing**, never a restatement of docs the stack's `SOURCES.md` row already serves — order that matters, defaults with the reason attached, traps, and what to leave out. Derivable-from-the-source-in-the-moment is a lookup, not a skill.

`/kru/roster` also does the inverse (`retire`), authors skills (`author`), and reports wiring **drift** (`audit`) — e.g. the agent count in `plugin.json`/`marketplace.json` disagreeing with `agents/*.md`.

Versioning: semver-ish. New agent or capability → **minor**. Prompt tweaks/fixes → **patch**. Breaking reorg of the orchestration contract → **major**.
