# Official Sources Registry

**Team principle — official sources first.** For any framework / library / API / CLI / cloud-service specifics, no agent answers from training data. Resolve it through the official MCP, skill, or plugin below. Prefer first-party sources over third-party blogs. Training data is a starting hypothesis to verify, never the answer.

**Corollary — a repo-authored skill is the layer the docs can't serve.** Docs are hammer and nails; they don't make the house come out right the first time. So a skill here carries **recipes and patterns earned by working with the thing** — order that matters, defaults chosen after the obvious one failed, cases where correct-by-the-docs is wrong in practice, popular advice that's wrong. Anything derivable from the official source in the moment is a **lookup, not a skill**, and stays in that stack's row below. A skill that restates docs ages out of sync and then outranks the source while stale — which is why quickstart-grade community packs get rejected rather than vendored (see the Drizzle row). Authoring rules: `skill/kru-roster/author.md`.

Agents: consult the row for the stack you're working in. The PM (the `lead` skill) uses this table to route and to wire any newly-minted specialist to its official source.

| Stack / domain | Official source (use this) | How |
|---|---|---|
| **Any library/framework/API** (universal fallback) | **Context7 MCP** | `resolve-library-id` → `query-docs`. Their global rule already mandates this. |
| **Svelte / SvelteKit** → `sveltekit-builder` (network boundary) + `svelte-ui-builder` (components) | **Svelte MCP** + `svelte:*` skills | `mcp__svelte__list-sections` → `get-documentation`; validate with `svelte-autofixer`; `svelte:svelte-core-bestpractices`. Optional `svelte:svelte-file-editor` agent. Both seats share this chain; the seam between them is data-agnostic components (serializable props + callbacks — see `ROSTER.md`). |
| **Python** (`pyproject.toml` / `.py`) — MCP servers, libraries, CLIs, packaging → `python-developer` | **`python -m pydoc` off the installed interpreter** → `docs.python.org` (library reference · `whatsnew`) + **the installed package's own source** → `modelcontextprotocol.io` + the `mcp` SDK migration guide / PyPI → Context7 | Same read-off-disk rationale as the Go and TanStack rows, and it bites harder here: nothing at runtime enforces an annotation, so the *installed* version's behavior is the only authority — `python -c "import x, inspect; print(inspect.getsource(x.thing))"` settles in one command what a changelog argues about. The stack's live hazard is the **`mcp` SDK 1.x → 2.x rename**: `uv add mcp` resolves to 2.x, where `FastMCP` is `MCPServer`, so nearly every published MCP example fails on a fresh install — read the lockfile's major before copying anything. The seat loads the repo-authored **`python` skill** first (`skill/kru-python/` — the failures that pass `ruff` and `mypy` and ship quiet; the MCP-server, packaging and test-mechanics halves in `reference/`), **harvested** from `wdm0006/python-skills` and **not vendored** — the pack's front halves restate docs this row already serves current, and verifying it corrected four of its MCP claims; see `ROSTER.md` → *Reused, not owned*. Schema stays the data architects' (SQL is language-agnostic); the Python that runs it is this seat's. Gate: `ruff check` **and** `ruff format --check` (different tools), `mypy` at the repo's strictness, `pytest` one-shot with a non-zero collection count. |
| **Go-served React app** (`go.mod` + a React SPA) → `go-fullstack-builder` (the whole request path: Go service + typed client) | **`go doc` off the installed toolchain** → `go.dev` (Effective Go · language spec · module docs) + `pkg.go.dev` → Context7 (`/golang/go`) | Same read-off-disk rationale as the TanStack row: `go doc <pkg>[.<Symbol>]` answers from the exact stdlib and module versions in `go.mod`, so it outranks any web copy — `go doc net/http.ServeMux` for method+pattern routing, `go doc embed` for serving the built SPA. `pkg.go.dev` for a third-party module's docs and examples, Context7 last. The React data layer (`@tanstack/react-query` / router) is Context7 on the version `package.json` names; components rendered inside the routes are `react-ui-builder`'s. Schema stays the data architects' (SQL is language-agnostic); the Go code that runs it is this seat's. Every API the seat names was checked with `go doc` on go1.26.1, and the `embed` failure on a missing/empty dir reproduced. The seat loads the repo-authored **`go` skill** first (`skill/kru-go/` — the traps that compile and ship quiet; security / `database/sql` / the gate / the version table in `reference/`), distilled from the 14 ⭐ skills of `samber/cc-skills-golang` and **not vendored** — the pack is mostly lookups this row already serves, and its per-skill fan-out orchestration blocks contradict the seat's no-self-dispatch rule; see `ROSTER.md` → *Reused, not owned* for what was kept and the five seat corrections it forced. Gate additions from the same pass: `-shuffle=on`, the `shadow` analyzer default `vet` omits, `govulncheck` on a `go.mod` change. |
| **React Router** (framework / data / declarative / RSC) → `react-router-builder` (network boundary) | **`react-router` skill** (vendored, official) → installed docs → Context7 | Invoke the `react-router` skill (mode-aware); it defers to `node_modules/react-router/docs/` as truth. Context7 (`react-router` v7) is the fallback. Prefer a repo's own `.agents/skills/react-router` if present. Components rendered inside routes are `react-ui-builder`'s (next row) — this seat mounts them behind serializable props + callbacks. |
| **TanStack Start** (React) / **TanStack Router** → `tanstack-start-builder` (network boundary) | **TanStack's own skills, shipped inside the installed packages** → `tanstack.com/start/latest/llms.txt` → Context7 | Read off `node_modules`, not the web: `@tanstack/react-start/skills/react-start/` (entry, plus its `server-components/` RSC subtree and `lifecycle/migrate-from-nextjs/`), `@tanstack/start-client-core/skills/start-core/` (the server boundary) and `@tanstack/router-core/skills/router-core/` (the routing concerns) — each entry `SKILL.md` carries the table naming its own sub-skills. **No vendored copy for this stack**, and that's the point: the shipped skills are version-matched to the app being edited, where a vendored tree would pin one release of a framework that ships weekly and then outrank the installed truth while stale (the corollary above, from the other direction — the docs *are* the source here, and the source travels with the install). Their loading rule binds the seat: entry skill + the one sub-skill for the boundary being changed. Greenfield installs first, then reads what the install put on disk; a repo's own `.agents/skills` copy outranks `node_modules`. Fallbacks in order: the docs index at `https://tanstack.com/start/latest/llms.txt`, then Context7 (`/websites/tanstack_start_framework_react`). Components rendered inside routes are `react-ui-builder`'s — this seat mounts them behind serializable props + callbacks. |
| **React UI components** (framework-agnostic pages/sections/interactive `.tsx`, mounted by RR7/Next routes) → `react-ui-builder` | **`vercel:react-best-practices`** skill + `ark-ui`/`modern-css` skills → **Context7** | Run `vercel:react-best-practices` as the component-craft quality pass (hooks/effects/keys/memo/rendering/TSX): ~80% of its rules are pure React/JS/DOM (apply verbatim); skip the `server-*` category (server code isn't this seat's lane), `next/dynamic` translates to `React.lazy` in RR7 repos. Component-library APIs via Context7, never memory — **shadcn is the exception**, with its own vendored skill (next row but one). The seam: data-agnostic components — serializable props + callbacks, no loader-data hooks/route types/server imports (see `ROSTER.md`). |
| **Vanilla custom elements / shadow DOM** (an embeddable widget for a page the team doesn't control, or a design system consumed by more than one stack) → `web-components-builder` | the repo-authored **`web-components` skill** (`skill/kru-web-components/`) + **`modern-html`**/**`modern-css`** → **MDN** for element/API behavior → **`api.webstatus.dev`** for Baseline status → **Context7** for any library the repo does bring in | The platform *is* the source here — there is no framework vendor to defer to, so MDN and the Baseline dataset outrank memory on every lifecycle, shadow-DOM and `ElementInternals` question, and the seat states which it used. Load the `web-components` skill first (it is the seat's playbook: upgrade order, the pre-upgrade property that shadows its accessor, `connectedCallback` re-firing on every move, targeted updates instead of `innerHTML`, what crosses the shadow boundary, `CustomEvent` defaults, `ElementInternals`, `AbortController` cleanup), then `reference/embedding.md` when the element ships into someone else's page. **No MCP and no community pack** — the relevant packs wrap a framework's element story, and the platform's own docs are complete. The seam vs. the other two UI builders is the **consumer**: UI inside a React or Svelte app is theirs. |
| **Postgres + drivers** (Drizzle / Prisma / postgres.js / pg / Kysely) → `postgres-architect` | **Context7**; for **Drizzle** the repo-authored **`drizzle` skill** (`skill/kru-drizzle/`) + its official **`llms.txt`** | Resolve the exact driver id before writing queries/migrations. Drizzle: load the `drizzle` skill first (it is the seat's playbook for the ORM layer — version split, reviewing generated SQL, `push` vs `generate`), then `https://orm.drizzle.team/llms.txt` (per-dialect schema/migrations/query + drizzle-kit + 40+ provider connections) as first-party index and Context7 (`/websites/orm_drizzle_team`, `/drizzle-team/drizzle-orm-docs`) for exact API. **Both official surfaces serve v1 content by default while npm `latest` is 0.45.x** — check the installed version before using either. Do **not** add third-party community skill packs for a driver already covered here (violates official-sources-first) — the `drizzle` skill is repo-authored and encodes verified findings the docs don't state, it does not restate them. Postgres-the-server only — an embedded SQLite file is `sqlite-architect`'s (next row), and the habits don't transfer. |
| **Embedded SQLite** (a local `.db` file: better-sqlite3 / `node:sqlite` / bun:sqlite, Drizzle's `sqlite` dialect) → `sqlite-architect` | the repo-authored **`sqlite` skill** (`skill/kru-sqlite/`) → **sqlite.org** for engine semantics → **Context7** for driver/ORM APIs | Load the `sqlite` skill for the playbook (connection pragmas, `IMMEDIATE` transactions, STRICT, the rebuild), then pull the one `reference/` file the task needs — `drivers.md`, `migrations.md`, or `ops.md`. **sqlite.org outranks everything for engine behavior** — pragma semantics, WAL, transaction locking, `ALTER TABLE`, `VACUUM INTO` — and is the reason the skill exists: the widely-copied pragma blocks in circulation trace to a handful of 2020 blog posts and carry real mistakes (`mmap_size=30000000000` trades away I/O error detection, `synchronous=OFF` can corrupt, `page_size` can't be changed once in WAL). Drivers via Context7 (`better-sqlite3`; `node:sqlite` is Node's own docs, RC as of v25). **Drizzle → the repo-authored `drizzle` skill** (`skill/kru-drizzle/`, + `reference/sqlite.md`) before `llms.txt`/Context7: drizzle-kit's generated table rebuild leaves foreign keys live during its `DROP TABLE` (its `PRAGMA foreign_keys=OFF` sits inside the migrator's transaction, where it is a no-op), which **deletes every `ON DELETE CASCADE` child row and reports success** — verified, and stated nowhere in the Drizzle docs. Drizzle also defaults transactions to `deferred` and cannot emit `STRICT`. **Three SQLite-shaped lanes, one owner each**: an embedded file the app opens = this seat; **D1** = `cloudflare-builder` (Workers bindings + migration CLI); **libSQL/Turso** = neither — a remote/replica server changes the concurrency story enough that these recipes mislead, so mint a seat if it recurs. Postgres is `postgres-architect`'s. |
| **Auth** (Better Auth) → `better-auth-specialist` | **Better Auth docs MCP** (`https://mcp.better-auth.com/mcp`) → official **Skills** → **`llms.txt`** → **Context7** (`/better-auth/better-auth`) | Prefer the docs MCP if `mcp__better-auth__*` tools are connected; install via `npx auth@latest mcp --claude-code` (or `mcp.json`: `{ "better-auth": { "url": "https://mcp.better-auth.com/mcp" } }`). Official agent skills: `npx skills add better-auth/skills`. Fallbacks: `https://better-auth.com/llms.txt` (index; `llms-full.txt` full) and Context7. Note: this docs MCP ≠ the Better Auth `mcp()` *library plugin* (which makes an app an OAuth/MCP provider — and which as of 1.7 ships from `@better-auth/mcp`, not core). **The docs MCP carries the canonical `/docs/guides/1-7-upgrade-guide`** — read it, not the blog post, before touching a repo on 1.6. |
| **Headless UI components** (Ark UI: React/Vue/Solid/Svelte) → the **`ark-ui` skill** (repo-authored, `skill/kru-ark-ui/`; loaded by the UI component builders `react-ui-builder`/`svelte-ui-builder` — Ark is ambient) | **Ark UI MCP** (`npx -y @ark-ui/mcp`) → **`llms.txt`** → **Context7** (`ark-ui`) | Load the `ark-ui` skill for the playbook (reach-for-Ark judgment, part anatomy, a11y, styling hookup). Install the MCP: `claude mcp add ark-ui -- npx -y @ark-ui/mcp`. Tools: `list_components`, `list_examples`, `get_example` (framework-aware), `styling_guide` (design tokens / `data-*` attrs / CSS vars). Fallback: `https://ark-ui.com/docs/ai/llms.txt`, then Context7. Ark is headless/unstyled on Zag.js state machines — apply the repo's tokens via the styling guide; compose the full part anatomy so the built-in a11y fires; don't hand-roll ARIA/focus. Brownfield: defer to the repo's existing component lib — and when that lib is **shadcn**, its own row below carries the playbook. Ark for Vue/Solid/Svelte, already-on-Ark, and greenfield React where the lead's plan names it. This single-purpose official docs MCP is fine (same shape as the Better Auth / Svelte MCPs) — no fleet. |
| **shadcn/ui components** (React/Tailwind, source-copied into the repo; the tell is a root `components.json`) → the **`shadcn` skill** (vendored, `skill/kru-shadcn/`; loaded by `react-ui-builder` — the second primitive library, ambient like Ark) | **vendored `shadcn` skill** ← `shadcn-ui/ui` `main:skills/shadcn` (shadcn's own first-party skill) → the **shadcn CLI** (`npx shadcn@latest`) → optional **shadcn MCP** → **Context7** | The skill *is* the source and it's self-refreshing where it counts: `!`npx shadcn@latest info --json`` runs as it loads, so `aliases`, `base` (`radix` vs `base-ui` — `asChild` vs `render`), `iconLibrary`, `tailwindVersion`, `tailwindCssFile` and the installed-component list come from the repo rather than from memory. Per-component API via `npx shadcn@latest docs <component>` → fetch the URLs; `search`/`view`/`add --diff` for registry work. Optional MCP for conversational registry install: `pnpm dlx shadcn@latest mcp init --client claude`. **Which library a greenfield React repo gets is the lead's call, stated in the plan** — the rule is written once, in the `ark-ui` skill's reach-for section, and never introduce the second library into a repo that has one. **In a shadcn repo the installed theme *is* the system** — Block E applies to it unchanged (read the names off `tailwindCssFile`, return a named gap), and stock `components/ui/*` stays as the CLI wrote it so the next `add --diff` has nothing to reconcile. Why vendored rather than `pnpm dlx skills add shadcn/ui` per project: the plugin has to work identically in Claude Code on the web, where nothing installs into `~/.claude`. |
| **Modern/native CSS** (which platform features are safe to reach for now — container queries, `:has()`, nesting, subgrid, color functions, `@starting-style`, scroll-driven/view transitions, `@scope`, …) → the **`modern-css` skill** (repo-authored, `skill/kru-modern-css/`; loaded by the UI component builders `react-ui-builder`/`svelte-ui-builder`/`web-components-builder` — ambient) | **web.dev/baseline** (the authoritative Widely/Newly/Limited-available status) → **MDN** (per-feature Baseline badge) → **caniuse.com** | Load the `modern-css` skill for the playbook: the three Baseline states and what each licenses, the aging rule (Baseline year + ~30 months ≈ Widely available — recompute against today, don't trust a hardcoded label), and the old-technique → native-CSS replacement table. Curated snapshot in `reference/baseline.md` covers Baseline 2023–2025 only; for anything shipped after 2025 or not in that list, check live status via WebFetch (`web.dev/baseline/<feature>`, MDN, caniuse) rather than guessing — Baseline status moves monthly and training data lags it. |
| **Modern/native HTML** (which native elements now replace a library or hand-rolled JS — `<dialog>`/`showModal()` vs the `popover` attribute, invoker commands, `<details name>`, `inert`, constraint validation + `:user-invalid`, `<output>`, `<search>`, loading/priority hints) → the **`modern-html` skill** (repo-authored, `skill/kru-modern-html/`; loaded by the UI component builders — ambient) | **`api.webstatus.dev`** (the Baseline/`web-features` dataset, queryable: `/v1/features?q=<term>` returns status + the Baseline low/high dates) → **web.dev/baseline** → **MDN** (per-feature Baseline badge) | Load `modern-html` for the judgment (native-first, the old-technique → native-element table, the traps) and `reference/baseline.md` for the grouped status table. It **defers to `modern-css` for the three Baseline states and the aging rule** rather than restating them — one source, both halves of the platform. Deliberately **not** a semantics primer: landmarks and heading order are things every model already writes correctly, so including them would fail the skill entry bar. Status was verified against the API, not recalled — the difference was material (Popover is **Newly, 2025-01-27**, not 2024; `:user-invalid` and `<search>` crossed to **Widely** in 2026), which is why the API is named first here rather than the human-readable pages. |
| **Web components / shadow DOM recipes** (the failures that don't throw) → the **`web-components` skill** (repo-authored, `skill/kru-web-components/`; `web-components-builder`'s playbook — **not ambient**, same posture as `sqlite`) | **MDN** (Web components, `ElementInternals`, `Event.composed`) + **web.dev** custom-element best practices + **`api.webstatus.dev`** for status; **WHATWG HTML** for constructor conformance | The skill carries what the docs state but scatter, plus the platform-status facts that shape the design rather than decorate it: **scoped custom element registries** and **reference target** are both Limited, so a tag name is a global with no escape and ARIA cannot cross a shadow root; `:host-context()` and customized built-ins (`is=`) are Limited; `adoptedStyleSheets` and form-associated custom elements have been **Widely available since 2023-03**. Two `reference/` files carry what only some runs need: `baseline.md` holds the full grouped status table (same curation as `modern-css`/`modern-html`), so `SKILL.md` keeps only the four Limiteds it has to design around. `reference/embedding.md` covers the third-party-embed half — the registration guard, the host's CSP (constructed stylesheets are *specified* to need `'unsafe-eval'`, but per MDN **no browser blocks them**), `position: fixed` under a transformed ancestor, third-party cookies, and the theming contract as a versioned public API. Where the platform doesn't answer, the skill says so instead of guessing: **mounting Stripe Elements inside a shadow root is unverified** (Stripe's docs are silent; `stripe/stripe-js` #143 from 2021 is closed with no documented resolution) and the instruction is to spike it before the architecture depends on it. |
| **Terminal UI** (a screen drawn into a terminal grid: Bubble Tea/Go, Ratatui/Rust, Textual/Python, Ink/React) → the **`tui-design` skill** (repo-authored, `skill/kru-tui-design/`; the conditional row in `references/routing.md` puts it into a brief, and `python-developer`/`go-fullstack-builder` carry the pointer beside their terminal-output one) | Each framework's own docs, resolved current: **pkg.go.dev** (`charmbracelet/bubbletea`, `lipgloss`, `bubbles`) · **docs.rs/ratatui** · **textual.textualize.io** · **Context7** (`/vadimdemedes/ink`, `/textualize/textual`, `/websites/ratatui_rs`; Bubble Tea's only entry is the thin `/websites/pkg_go_dev_...` mirror, so Go resolves through `go doc` off the installed module instead), plus the registries (crates.io, PyPI, npm, `proxy.golang.org`) for what version a claim holds on | The skill is the layer the docs don't carry — **cell-width math** (a rune is not a cell; `go-runewidth` reads the locale and an East-Asian one flips ambiguous-width glyphs, including box-drawing, to two cells, so `RUNEWIDTH_EASTASIAN=0` is the fix for a frame that shreds in one shell and not another), **color on a background you must detect** (Lip Gloss v2 replaced v1's `AdaptiveColor` with `HasDarkBackground` + `LightDark`; v1's forms survive under `compat` only), the **stale-response race** and where each framework already solves it (`@work(exclusive=True)` in Textual; a generation counter everywhere else), and the **exit path** — the one place the popular advice is actively wrong: Bubble Tea catches panics and cleans up by default (a hand-rolled `recover` fights it) and `ratatui::init()` installs its own restoring hook, whose documented ordering constraint is that it must be installed **after** `color-eyre` or the report prints into an altscreen nobody sees. Three `reference/` files, one per branch: `frameworks.md` (the four stacks, the companion sets that appear in the same manifest, and the Charm **v2 import-path split** that makes pkg.go.dev's default page describe v1), `robustness.md` (size gate, bounded streams with a visible drop count, backoff, failure→behavior, golden frames), `visual.md` (the emphasis ladder, what earns a border, ASCII fallback). Vendoring `pageton/tui-design-skill` was rejected — **no license** on the repo, and its framework pages are quickstarts Context7 serves more currently. |
| **House UI patterns** (component-level *behavior* indexed by *what you're about to build*: when a form validates and where a failed submit sends focus, where a mutation reports its outcome, what a repeated row control announces, an icon beside a wrapping label, prose restating the UI) → the **`ui-patterns` skill** (repo-authored, `skill/kru-ui-patterns/`; loaded by the UI component builders `react-ui-builder`/`svelte-ui-builder`/`web-components-builder` — ambient — by the three framework builders for `forms-and-mutations`, and by `ux-designer` when it specs a component) | **No external source, by construction** — the corpus is the team's own swept preferences (`PREFERENCES.md` → `/kru-roster learn`), which is why it carries an entry bar instead of a citation chain | Load the **one group** the index maps your build target to, never the corpus — most patterns don't apply to most components, and loading all three is the failure mode the index exists to prevent. Two bars gate an entry, both in `CURATION.md` (authoring-only — `SKILL.md` doesn't point at it, so it costs nothing at build time): it names the **default it corrects**, so a pattern a careful builder would produce anyway is padding and stays out; and it is **design-system agnostic** — behavior, semantics or grouping that holds under any token file — so the corpus and the design system can't contradict each other. Four things stay out of it and are named in *Owned elsewhere*: **appearance** (the project's token file, always — including what a focus indicator or a selected row looks like), **Baseline safety** (`modern-css`), **primitive anatomy** (`ark-ui`), **whether the journey works** (`ux-principles`). A pattern needing a value the system doesn't name is a **named gap**, same as any other. |
| **Panda CSS** (build-time CSS-in-JS: `panda.config.*` + a generated `styled-system/`) → the **`panda-css` skill** (repo-authored, `skill/kru-panda-css/`; loaded by the UI component builders `react-ui-builder`/`svelte-ui-builder` **when the repo uses Panda** — conditional, not ambient) | **Panda MCP** (`npx -y @pandacss/mcp`) → **`llms.txt`** (`https://panda-css.com/llms.txt`) → **Context7** (`panda-css`) | Load the `panda-css` skill for the playbook — what the extractor can resolve (in-file literals only; it reads source text, never types), the config recipe whose runtime variant emits a class with **no rule**, `cx` conflicts decided by sheet order rather than argument order, `include` globs that silently skip `app/`/`.svelte`, `strictTokens` as a type-only gate, and `_dark` matching `.dark` alone. Install the MCP: `claude mcp add panda -- npx -y @pandacss/mcp` — it answers against **this repo's resolved config** (`get_tokens`, `get_recipes`, `get_conditions`, `get_config`, `get_usage_report`), which no docs page can. npm `latest` is v1 and the docs document v1; `2.0.0-beta.x` (Rust/Oxc compiler rewrite, bundler plugins) is opt-in on the `beta` tag — check `package.json` first. Brownfield rule: never introduce Panda into a repo that already has a styling system. |
| **3D / WebGL–WebGPU** (three.js in a web app: canvas mount + lifecycle, the component layer, TSL shaders, GPU compute, post-processing) → ambient in the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`) | **Context7** — `/llmstxt/threejs_llms-full_txt` (official, TSL/WebGPU-aware) → `/mrdoob/three.js` for core API; `/threlte/threlte` (Svelte) or `@react-three/fiber` + `drei` (React) for the component layer — plus the **vendored `webgpu-threejs-tsl` skill** (`skill/kru-webgpu-threejs-tsl/`) for TSL/WebGPU/compute | **Build through the framework wrapper, not vanilla three** — Threlte on Svelte, R3F + drei on React: the wrapper owns mount/lifecycle/reactivity, three owns the scene objects, and the seam stays the team's usual one (serializable props + callbacks; the scene graph is a component tree, not an imperative `init()`). Resolve every API via Context7, never memory — three moves fast and training data lags it (r155 color management, r171+ stable TSL, r178 `PI2`→`TWO_PI`). Load the vendored `webgpu-threejs-tsl` skill for node materials, TSL, GPU compute, WGSL interop, and device-loss handling; it is a **narrow supplement, not the source of truth** — an official three.js source exists, so Context7 outranks it wherever they overlap (unlike the motion/marketing packs, which back domains with no first-party source). Quality floor: **client-only** (three touches `window`/WebGL — guard SSR, `onMount`/`ssr: false`), **dispose geometries/materials/textures + the renderer on unmount** (WebGL contexts leak and are capped per page), cap `setPixelRatio` at ~2, and honor `prefers-reduced-motion` by parking the render loop. WebGPU needs `await renderer.init()` and a capability check with a WebGL fallback — it is not yet Baseline (check via `modern-css`'s aging rule). Hero/ambient *non-interactive* art stays `graphic-designer`'s (Veo video / p5.js generative) — this row is for interactive 3D that ships as components. |
| **TypeScript** (language + type system + compiler config) → the **`typescript` skill** (repo-authored, `skill/kru-typescript/`; loaded by every TS-writing builder — TS is ambient) | **Context7** (`typescript` — pin the installed major) + the official **tsconfig reference** (`typescriptlang.org/tsconfig`) + the compiler's self-docs; curated baseline: Matt Pocock's **tsconfig cheat sheet** (`totaltypescript.com/tsconfig-cheat-sheet`) | Load the `typescript` skill for the playbook. Confirm every tsconfig option and type-system behavior against docs, never memory. Seed a *fresh* tsconfig from the cheat-sheet baseline (base: `target es2022`, `moduleDetection force`, `isolatedModules`, `verbatimModuleSyntax`, `esModuleInterop`, `skipLibCheck`; strict: `strict` + `noUncheckedIndexedAccess` + `noImplicitOverride`; bundler → `module preserve`+`noEmit`, tsc-emit → `module nodenext`), then verify each option against the reference. Read the *effective* config/cost: `tsc --showConfig` (extends chains hide options), `tsc --explainFiles`, `--extendedDiagnostics`/`--generateTrace` for slow checks. **Extend the framework's generated tsconfig** (SvelteKit `.svelte-kit/tsconfig.json`, Next's `next` plugin, `astro/tsconfigs/*`, `@tsconfig/*`) — don't fork it. Covers the compiler config, the type layer (generics/conditional/mapped/branded types, discriminated unions, inference, `.d.ts`, `declare module`), ESM/CJS, monorepo project references (`tsc -b`), JS→TS migration, and the typecheck gate (`tsc --noEmit`/`svelte-check`); **excludes** the **formatter/linter and the monorepo task/package graph** (Biome/ESLint/Prettier + pnpm + Turborepo — that's the **`toolchain-engineer`** seat's, next row). Repo-wide TS-infra work = the lead dispatches a general agent with this skill. |
| **Repo tooling / build orchestration** (pnpm workspaces + Turborepo task graph/caching + Biome/ESLint/Prettier lint/format) → `toolchain-engineer` | **pnpm docs** (`pnpm.io`) + **vendored `turborepo` skill** (`skill/kru-turborepo/`) + **Biome docs** (`biomejs.dev`, `llms.txt`) + **Context7** (`pnpm`, `turbo`, `biome`; `eslint`/`prettier` for brownfield) | Owns the tooling substrate, not app code: `pnpm-workspace.yaml`/catalogs/lockfile → `turbo.json` (tasks, `dependsOn`, `outputs`, cache keys, env passthrough, boundaries) → `biome.json`. Layering: pnpm (package graph) → Turbo (task graph) → Biome (quality gate). Seam with `typescript`: that skill owns `tsconfig` *content* (incl. `tsc -b` project references); this seat owns the `typecheck`/`lint`/`build`/`test` **tasks** that run it + their cache inputs. pnpm is the team greenfield default (the `lead` skill) — this seat owns that default's config thereafter; **brownfield: match the repo's actual PM + linter, never rip-and-replace**. Biome's own `.claude/skills` are contributor-facing (developing Biome) — not used here. |
| **Schema validation** (Zod 4 at a parse boundary: request/form/env/params) → the **`zod` skill** (repo-authored, `skill/kru-zod/`; loaded by the network-boundary seats `sveltekit-builder`/`nextjs-builder`/`react-router-builder`/`tanstack-start-builder`/`cloudflare-builder` **when the repo has `zod`** — conditional, not ambient) | **`https://zod.dev/llms.txt`** (index) + **`llms-full.txt`** (full text, v4-native) → **Context7** (`zod`) | Load the `zod` skill for the playbook — the traps that **return a value instead of throwing**: `.default()` skipping every check on its own schema (`.prefault()` parses it), `z.coerce.boolean("false") === true` (`z.stringbool()`), `safeParse` **throwing** `$ZodAsyncError` on a schema with an async refinement, `.catch()` catching ZodErrors but not a throw inside `.transform()`, transform output nothing validates until `.pipe()`, `flatten()` filing nested errors under the top-level key (`z.treeifyError`/`z.prettifyError` keep the path), and `toJSONSchema` defaulting to the *output* view so defaulted fields come out `required`. Recipes disclosed to `reference/errors.md` (customization ladder, `.check()`, when an object-level `.refine()` is skipped) and `reference/boundaries.md` (env/form-data/JSON/search-params, the `""`-isn't-`undefined` trap). Authored rather than vendoring the skills.sh `zod` pack, which is **rejected** on the same grounds as the Drizzle row: it ships `zodVersion: "4.x"` metadata while teaching Zod 3 spellings throughout, and its flagship error-handling rule documents `flatten()` producing `'profile.name'` keys — verified false, the key is `profile`. Every claim in the skill reproduced against `zod@4.4.3`, not release notes. Form-library wiring (RHF resolvers) stays the framework seat's, and **Superforms has its own row below** (the `superforms` skill picks up where this one hands the `ZodError` over); `drizzle-zod` is the `drizzle` skill's. |
| **Schema validation** (Valibot 1.x at a parse boundary: request/form/env/params) → the **`valibot` skill** (repo-authored, `skill/kru-valibot/`; loaded by the network-boundary seats `sveltekit-builder`/`nextjs-builder`/`react-router-builder`/`tanstack-start-builder`/`cloudflare-builder` **when the repo has `valibot`** — conditional, not ambient) | **`https://valibot.dev/llms.txt`** (index; `llms-full.txt`, `llms-api.txt`; every docs page serves Markdown at `.md`) → the **first-party MCP server** (`claude mcp add --transport http valibot https://valibot.dev/mcp`) → **Context7** (`valibot`) | Load the `valibot` skill for the playbook — the traps that **never run or return a value**: `v.is()`/`v.assert()` over an async schema returning `true` and compiling clean under `strict` (`tsc` catches `parse`/`safeParse`/a sync `object` over the same schema; at runtime those return `undefined`, `{ success: true }`, and an object with the async key dropped), `v.toBoolean("false") === true` and `v.toNumber("") === 0`, a failed `safeParse` with `typed: true` and `output` populated, `fallback` swallowing a failed validation into an unvalidated value while `optional`'s default *is* validated, `optional` rejecting `null`, `union` of objects reporting "Expected Object but received Object", and `toJsonSchema` throwing on the first transform. Recipes disclosed to `reference/boundaries.md` (env / form data via `decode-formdata` / JSON / search params, and the Standard Schema seam into Conform, React Hook Form, Superforms, TanStack) and `reference/errors.md` (issue shape, `flatten`'s dot paths, `forward` + `partialCheck`, the message ladder, `abortEarly`/`config`). Every claim reproduced against `valibot@1.4.2`; why it is authored rather than vendored from Valibot's own agent `SKILL.md` is its `ROSTER.md` entry. Form-library wiring past the adapter stays the `conform`/`react-hook-form`/`superforms` skills'; `drizzle-valibot` is the `drizzle` skill's; a repo on Zod loads the row above. |
| **SvelteKit forms** (Superforms 2: `superValidate` in an action, `superForm` in a component) → the **`superforms` skill** (repo-authored, `skill/kru-superforms/`; loaded by `sveltekit-builder` + `svelte-ui-builder` **when the repo has `sveltekit-superforms`** — conditional, not ambient) | **`https://superforms.rocks/`** (first-party, v2-native) → **Context7** (`sveltekit-superforms`) | Load the `superforms` skill for the playbook — the traps that report **`valid: true` about data nobody entered**: an absent field filled from its JSON-Schema type before validation (`enum → its first member`, so an empty POST against `['admin','editor','viewer']` validates as `admin`; `strict: true` fixes it and breaks every checkbox unless each boolean also gets `.default(false)`), a nested `user.name` **discarded** while the generated default passes in its place (`dataType: 'json'`, which needs JS + Superforms' own `use:enhance`), a form **id that's a hash of the schema shape** so two structurally identical schemas collide on one page, client proxies writing `NaN`/`Invalid Date` that `JSON.stringify` renders as `null` and `"1,5"` that becomes `1`, and **`withFiles` being the same function as `removeFiles`** (both delete). Recipes disclosed to `reference/actions.md` (the canonical action, the `message`/`setError`/`fail` return ladder, multiple forms, GET forms, the file round trip) and `reference/client.md` (`superForm` defaults read out of the shipped `dist/`, event order, the proxy table, componentization, SPA mode). **No `llms.txt` exists** and the v1 docs live on a separate host that answers v1 questions as if current — check the installed version first. Every claim reproduced against `sveltekit-superforms@2.30.2`. The Zod schema underneath stays the **`zod`** skill's; route structure and the component seam stay the two Svelte seats'. |
| **React forms** (Conform 1.x: `parseWithZod` in an action, `useForm` in a component) → the **`conform` skill** (repo-authored, `skill/kru-conform/`; loaded by `nextjs-builder` + `react-router-builder` + `tanstack-start-builder` + `react-ui-builder` **when the repo has `@conform-to/react`** — conditional, not ambient) | **`https://conform.guide/`** (first-party) → **Context7** (`@conform-to/react`) | Load the `conform` skill for the playbook — its parse **deletes rather than invents**, so every trap is a value that quietly went missing: `""` stripped to `undefined` before validation, which makes `.min(1, 'Name is required')` report Zod's raw type error instead and turns `.default()` on a cleared field into `status: 'success'`; an absent array validating as `[]`; `z.instanceof(File)` passing the empty file input that `z.file()` rejects; `user[name]` dropped from the payload silently; an **intent submission** (every list button and every no-JS `reset`) parsing to `status: undefined` with an empty `error` and no `value`, so only `!== 'success'` guards the write; and `reply()` **echoing the submitted password back into the rendered HTML** unless `hideFields` names it. Recipes disclosed to `reference/server.md` (the action, the return ladder, intents without JS, the two-phase async check, uploads, the future-API server shape) and `reference/client.md` (`useForm` defaults read out of the shipped `dist/`, helper output, the a11y wiring the helpers leave to you, dynamic lists, UI-library controls, the framework mounts). **No `llms.txt` exists** (404) and the site teaches **two APIs at once** — a preview `future` half that breaks on minor bumps, already taught by the integration guides — so check which half a page is on before copying. Every claim reproduced against `@conform-to/react@1.21.1` with `zod@4.4.3`. The Zod schema underneath stays the **`zod`** skill's; route structure and the component seam stay the framework and UI seats'. |
| **React forms** (React Hook Form 7: `useForm` + `register` in a component, the resolver at the schema seam) → the **`react-hook-form` skill** (repo-authored, `skill/kru-react-hook-form/`; loaded by `nextjs-builder` + `react-router-builder` + `tanstack-start-builder` + `react-ui-builder` **when the repo has `react-hook-form`** — conditional, not ambient) | **`https://react-hook-form.com/docs`** (first-party) → **Context7** (`/react-hook-form/documentation`, `/react-hook-form/resolvers`) | Load the `react-hook-form` skill for the playbook — RHF keeps the form outside React and **everything it does is an opt-in**, so each trap is a value that is present and wrong rather than missing: `formState` is a Proxy that subscribes only to properties **read during render**, so `disabled={!formState.isDirty \|\| !formState.isValid}` short-circuits past `isValid` and the submit button never enables (reproduced — a component reading no `formState` property re-renders **zero** times across two keystrokes, so the error markup never appears either); `<input type="number">` submits the string `"42"` and a `z.number()` blames the schema, while `valueAsNumber` on a blank input yields `NaN` that `JSON.stringify` writes as `null`; two same-named checkboxes submit `["x"]` when one is checked and **`false`** when none are, so the empty multi-select reaches a `z.array()` as a boolean; `register('x', { disabled: true })` drops the key from the payload entirely; `z.coerce.number()` turns a blank required field into a clean `0`; `setValue` writes a value the form still reports as pristine, valid and untouched; and **`handleSubmit` calls `preventDefault()` unconditionally**, so `<form action={serverAction} onSubmit={handleSubmit(...)}>` validates, succeeds, and never runs the action — while `action` alone skips validation and hands the server a `FormData` missing every `Controller` field. Recipes disclosed to `reference/wiring.md` (the submit ladder, server actions and progressive enhancement per framework, async/late defaults and `keepDirtyValues`, typing the three generics, field-vs-`root` server errors) and `reference/fields.md` (`useFieldArray`, `Controller`, the a11y markup `register` leaves to you, and where `watch`/`useWatch`/`useFormState` land their re-renders). **No `llms.txt` exists** (404), and the docs are accurate about shapes while quiet about cost — they state the Proxy subscription rule without the render counts and `isValid`'s tie to `mode` without the validation runs subscribing turns on (reproduced: **0** resolver runs unread, **3** read). One resolver path, not two — `zodResolver` covers Zod 3 and Zod 4 from a single export, detecting v4 by the schema's `_zod` property. Every claim reproduced against `react-hook-form@7.87.0` + `@hookform/resolvers@5.9.1` with `zod@4.5.4`, `react@19.2.8`. The Zod schema underneath stays the **`zod`** skill's; route structure and the component seam stay the framework and UI seats'. A repo already on **Conform** stays on Conform — the row above is its playbook. |
| **State machines / actor model** (XState 5: `setup()`/`createMachine`/`createActor`, and its React bindings `@xstate/react`) → the **`xstate`** skill (repo-authored, `skill/kru-xstate/`) + the **`xstate-react`** skill (repo-authored, `skill/kru-xstate-react/`) — both loaded by **`react-ui-builder`** **only when the repo has the matching package** (`xstate`, `@xstate/react` respectively) — conditional, not ambient | **`https://stately.ai/llms.txt`** (first-party, indexes both a stable `/docs/xstate*` tree and a parallel `/docs/xstate/v6/*` tree) → **Context7** (`/statelyai/docs`) | Load `xstate` for the core actor-model playbook — an invoked promise actor's rejection with no `onError` propagating up as an **uncaught exception on the process**, not a caught in-machine error; an `always` transition with no exit condition looping until the process **OOMs** (reproduced); `invoke` torn down and recreated fresh on every re-entry to its state where `spawn` (held in `context`) survives; and sequential `assign` calls resolving against each other's writes, in order. Load `xstate-react` for the hook seam on top — `useMachine`'s `input` read once at creation and never again (reproduced, same shape as `react-hook-form`'s `defaultValues`-at-mount trap), an inline machine definition getting torn down and recreated **every render** (`logic.config !== currentConfig`), `useSelector`'s default `===` compare re-rendering on every snapshot when the selector returns a new array/object (reproduced: 2 unrelated updates → 2 extra renders unstable, 0 stable — `shallowEqual` is the package's own fix), and a crashed actor throwing **during render** on the next read, which only an Error Boundary stops. **Mind the version split before installing anything**: `stately.ai`'s own `llms.txt` links `/docs/xstate/v6/quick-start`, whose install line is `npm install xstate@alpha` (npm dist-tag `alpha`) — but `@xstate/react@latest` (6.1.0) pins `peerDependencies: { xstate: "^5.28.0" }`, so following that page in a repo using both packages breaks the peer range outright. Both skills authored rather than left to the docs because the actor model's failures are structurally quiet — an actor's error doesn't throw where it happened, it throws the next time anything reads the actor, and an eventless-transition bug produces no error at all until memory runs out. Every claim reproduced against `xstate@5.32.6` + `@xstate/react@6.1.0` + `react@19.2.8`. Scoped to `react-ui-builder` alone (unlike `conform`/`react-hook-form`'s four-seat wiring): the verified use here is component-level (wizards, players, multi-step flows), not a network boundary — a server-orchestrated use would need its own research before wiring the boundary seats in. |
| **Next.js** (App Router) → `nextjs-builder` (network boundary) | **`vercel:*` skills** + Vercel MCP | `vercel:nextjs`, `vercel:next-cache-components`, `vercel:next-upgrade`; Context7 (`next.js`) fallback. Components are `react-ui-builder`'s (its row carries `vercel:react-best-practices` + `vercel:shadcn`-adjacent lib craft) — this seat mounts them behind serializable props + Server-Action callbacks. |
| **Web performance** (CWV, caching, rendering, bundle) → `vercel-perf-optimizer` | **`vercel:*` skills** + Vercel MCP | `vercel:performance-optimizer`, `vercel:next-cache-components`, `vercel:runtime-cache`; MCP `get_runtime_logs`/`get_deployment_build_logs` for real data. |
| **Vercel platform-ops** (deploy/CI-CD, env/secrets, Functions/edge runtime, Cron, domains, Firewall/WAF, AI Gateway, Marketplace/storage provisioning, Routing Middleware) → `vercel-platform-engineer` | **`vercel:*` skills** + Vercel MCP | `vercel:deployments-cicd`, `vercel:vercel-cli`, `vercel:env-vars`, `vercel:vercel-functions`, `vercel:vercel-firewall`, `vercel:routing-middleware`, `vercel:ai-gateway`, `vercel:workflow`, `vercel:marketplace`, `vercel:vercel-storage`; MCP for real project/deploy state. **Three Vercel-touching seats, three lanes**: app code = `nextjs-builder`; web perf/CWV/caching-for-speed = `vercel-perf-optimizer`; deploy/infra/security = this seat. The caching overlap: perf owns cache *decisions for speed*, this seat owns the deploy/runtime-cache *plumbing*. |
| **Sanity CMS** → `sanity-builder` | **`sanity:*` skills** + Sanity MCP | `sanity:sanity-best-practices`, `sanity:content-modeling-best-practices`; `get_schema` before querying. |
| **Cloudflare** (Workers, Pages, D1, KV, R2, Durable Objects, Queues, Workflows, Vectorize, Hyperdrive) → `cloudflare-builder` | **Cloudflare MCP** + **vendored `cloudflare` + `wrangler` skills** (`skill/kru-cloudflare/`, `skill/kru-wrangler/`) + Context7 | MCP (`mcp__claude_ai_Cloudflare_Developer_Platform__*` + `search_cloudflare_documentation`) for live account state + docs; the vendored `cloudflare` skill (SKILL.md indexes `references/` per product, loaded on demand) + `wrangler` skill for CLI/config; Context7 (`cloudflare-workers`, `wrangler`) fallback. **D1 (CF's SQLite) is this seat's** — the binding, migration CLI, and request model are Workers-shaped, so it isn't `sqlite-architect`'s embedded-file lane; Postgres-from-a-Worker = Hyperdrive in front of `postgres-architect`'s DB. |
| **Fly.io** (Machines, `fly.toml`, the image/`Dockerfile`, Volumes, secrets, deploy + `release_command`, regions, private networking, MPG/Tigris provisioning) → `fly-platform-engineer` | **flyctl MCP** (`fly mcp server --claude`, built into flyctl) + **`https://fly.io/llms.txt`** → **Context7** (`/websites/fly_io`) | MCP first for live account state (`apps`/`machine`/`volumes`/`secrets`/`status`/`logs`/`certs`/`orgs`) — it ships inside flyctl and is marked **experimental**, so verify the surface it exposes rather than assuming CLI parity. Docs via `llms.txt` → the specific page (`/docs/reference/configuration/` for fly.toml, `/docs/machines/`, `/docs/volumes/`, `/docs/mpg/`, `/docs/blueprints/` for task-shaped patterns); Fly ships **no `.md` twins**, so the HTML page is the read. Context7 (`/websites/fly_io`, ~11.7k snippets, High reputation) as fallback. **No skill authored and none to vendor**: Fly ships no first-party skills pack, and its docs answer fly.toml/flyctl/Machines in the moment — a lookup, not a skill (`skill/kru-roster/author.md`). The earned traps live in the seat prompt, each verified at the source rather than recalled: **`fly postgres` provisions an unmanaged cluster Fly Support does not support** while `fly mpg` is Managed Postgres; Volumes are single-server and unreplicated, and **`fly scale count` creates *empty* volumes rather than copying data**; `fly secrets set` restarts Machines and secrets never reach the image build; `release_command` runs in a temp Machine **with no volume access** and a 5-minute default kill, non-zero exit stopping the deploy; `canary`/`bluegreen` are unavailable with attached volumes. **Three platform lanes, one owner each**: Vercel = `vercel-platform-engineer`, the Workers runtime = `cloudflare-builder`, a long-lived VM with a disk = this seat. Schema on Fly MPG stays `postgres-architect`'s; an embedded `.db` on a Fly Volume stays `sqlite-architect`'s — this seat owns the volume under it and owes them the unreplicated-storage caveat that changes their backup story. |
| **Container images** (the `Dockerfile` an app ships in, BuildKit, base-image choice) → `fly-platform-engineer` | **`docker build --check`** (the gate) + **Context7** (`/docker/docs`) → **docs.docker.com** | **No skill authored, and the toolchain is why.** Docker ships its own linter: `docker build --check` (Dockerfile 1.8+) is a dry-run lint pass over the Dockerfile and build options, and `# check=error=true` promotes violations to build failures. Its rule catalog already covers what a best-practices skill would teach — the flagship example in Docker's own docs is `CMD node index.js` → `JSONArgsRecommended`, the shell form that leaves the process off PID 1 and swallows SIGTERM. A skill restating those rules is a **cache of a check the toolchain runs for free**, and it goes stale while outranking the linter (`skill/kru-roster/author.md`; `writing-for-agents` → the environment is a source of truth). Multi-stage builds, cache mounts, build secrets, non-root `USER`, `.dockerignore` are lookups — Context7 `/docker/docs` (~22k snippets, High reputation) serves them current. Community Docker skill packs are **rejected** on the Drizzle/zod precedent: quickstart material the source already answers. The one fact that isn't a lookup rides in the seat prompt instead, because it's a base-image *consequence* rather than a Docker feature: **alpine ships musl, not glibc** — `nodejs/docker-node` lists musl amd64 builds at support tier *Experimental* and ships arm64 musl untested, and glibc-built binaries (native modules included) don't run there, which is why the most-copied Dockerfile advice in circulation is the wrong default. |
| **Payments + billing** (Stripe: Checkout Sessions, Billing/subscriptions, Connect, webhooks) → `stripe-specialist` | **`docs.stripe.com/llms.txt`** → the **`.md` twin** of any docs page → **`stripe/stripe-node`** on GitHub → **Context7** (`/stripe/stripe-node`, `/websites/docs_stripe_com`) | **Deliberately no MCP** — Stripe's docs are served as static markdown, so the seat needs no server and no auth (this is the one integration row that doesn't route to a connected MCP). `llms.txt` carries a first-party **"Instructions for Large Language Model Agents"** section that is normative for the seat: **Checkout Sessions API is the default backend object** (never Charges), **Payment Element or Checkout** on the frontend (never the legacy Card Element), SetupIntent to save a payment method (never Sources/Tokens), dynamic payment methods over a hardcoded `payment_method_types`, Billing APIs for recurring revenue, and **Accounts v2** (`POST /v2/core/accounts`) for new Connect platforms — never the legacy `type: express\|custom\|standard`. Every docs page serves verbatim markdown at `<url>.md` (`webhooks.md`, `checkout/fulfillment.md`, `api/errors.md`) — **fetch that, never the HTML**, which comes back summarized with rows dropped. `gh api repos/stripe/stripe-node/…` for SDK shapes the prose skips (e.g. `constructEventAsync` for edge/Workers/Deno, where the sync verifier's Node crypto isn't there). **Never pin a version from memory** — Stripe's own llms.txt opens with that instruction; run `npm view stripe version`. |
| **Payments + billing** (PayPal: Orders v2, Catalog Products + Billing Plans + Subscriptions, webhooks, refunds) → `paypal-specialist` | **`developer.paypal.com/llms.txt`** → the **`.md` twin** of any docs page → **`paypal/paypal-rest-api-specifications`** (`openapi/*.json`) → the official SDKs (**`@paypal/paypal-server-sdk`**, **`@paypal/paypal-js`**) → **Context7** | The root `llms.txt` is an **index of indexes** — nine sections, each with its own `llms.txt` running to hundreds or thousands of pages — so the seat reads root → section → page and never pulls a section whole. Subscriptions sit under `/revenue/`, not `/payments/`. Every docs page serves verbatim markdown at `<path>.md` with **no trailing slash** — a trailing slash before `.md` returns 406, and the HTML is a JS-rendered app shell. The OpenAPI repo is the request/response ground truth and what generates types; it publishes **no v1 Payments spec**, which is why a repo handling the legacy `sale` resource vendors its own. **PayPal's official MCP is not a documentation server** (`@paypal/mcp` local, `mcp.paypal.com` remote) — it performs merchant operations against a real account under that account's credentials, so it answers no design question and is a one-way door pointed at production; deliberately absent from the chain. |
| **Integrations** (Linear, Notion, Drive) | their **official MCP** | Use the connected MCP server, not scraped docs. Stripe and PayPal have their own rows above — both are docs-first, and PayPal's MCP is a merchant-operations tool rather than a documentation source. |
| **Image/asset generation** → `graphic-designer` | **`scripts/gen-asset.ts`** (`@google/genai`) + Context7 (`/googleapis/js-genai`) | Run the bundled gen script (`npm --prefix "${CLAUDE_PLUGIN_ROOT}" run gen-asset -- …`); needs `GOOGLE_API_KEY`. Verify model ids/params via Context7, never memory. |
| **Ambient hero video** → `graphic-designer` | **`scripts/gen-asset.ts --video`** (Veo via `@google/genai`) + `ffmpeg` | Async Veo generation → webm + mp4 + poster still; needs `GOOGLE_API_KEY` and `ffmpeg`/`ffprobe` on PATH. Verify Veo model ids/params via Context7 (`/googleapis/js-genai`), never memory. |
| **True-alpha cutout / background removal** → `graphic-designer` | **`scripts/gen-asset.ts --cutout`** (rembg, local) | `--cutout --input <img>` → real 4-channel-alpha webp/png; runs fully local (no API key), self-bootstraps a python venv on first use. `u2net_human_seg` for people, `--rembg-model u2net` for objects. Do NOT use a gemini `--input` edit for cutouts — it recomposites to an opaque matte. |
| **Code-generated generative art** (p5.js flow fields / particles / parametric) → `graphic-designer` | **vendored `algorithmic-art` skill** (official Anthropic skill; `skill/kru-algorithmic-art/`) + Context7 (`p5.js`) | Invoke `/algorithmic-art` for the playbook — it emits a philosophy `.md` + generative `.js` + interactive `.html` viewer (code, not baked media). The **branch by aesthetic**, not a video substitute: photoreal/organic atmosphere → Veo video (row above); geometric/interactive/lightweight/seeded/brand-parametric → algorithmic-art. Bind the philosophy to the project's token file — solid values read by name (`--primary`, `--accent`, `--muted`), never opacity-tinted; seed for reproducibility. Verify p5.js API via Context7, never memory. |
| **Technical SEO + AEO** (metadata, OG, sitemaps/robots, canonical/hreflang, JSON-LD, AI-answer readiness) → the **`seo-review` skill** (user-invoked, runs inline) | **`sanity:seo-aeo-best-practices` skill** (general, despite namespace) + the stack's meta API | `/seo-review` loads the checklist skill, audits by layer (indexability → discoverability → rich presentation → international → answerability), and verifies the framework mechanism via its row (Next.js → `vercel:nextjs`/Vercel MCP for the Metadata API + `sitemap.ts`/`robots.ts`; others → Context7 for `<svelte:head>`/Astro head). Validate JSON-LD against schema.org. Report-first; markup fixes applied on user request. No Anthropic-native SEO skill exists (checked). CWV is `vercel-perf-optimizer`'s. |
| **Design token contract** (semantic color names, state steps, dark mode) → transcribed from what the design turn settled, written by the UI builders' Phase 0, and enforced by the repo's own conformance gate at every commit | **shadcn — "Theming"** (`https://ui.shadcn.com/docs/theming`) | The *naming convention, not the library*: plain custom properties under `:root` + `.dark` — `--background`/`--foreground`, `--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive` (each with its `-foreground`), `--border`, `--input`, `--ring`, `--chart-*`, `--sidebar-*`, `--radius`. **No shadcn or Tailwind dependency implied** (a Tailwind v4 repo may add the `@theme inline` mapping; primitives stay Ark's). Restrictive on purpose: a closed ~30-name vocabulary makes "is this value in the system" greppable, and adding a name is a system change. **The contract fixes the vocabulary, never the values or the technique.** Whether a hover step is a named `--primary-hover`, an alpha step like `bg-primary/90`, or a `color-mix()` is the design turn's decision, transcribed — the team holds no opinion on the CSS and no seat overrides the creative return, on accessibility or anything else. Two additions are available rather than mandated: state steps for each interactive token, and `--success`/`--warning` (+ `-foreground`) where the inventory shows states `--destructive` can't carry. **A repo that already spells the convention differently wins** — a shipped system on `-fg` rather than `-foreground` is the authority, and rewriting it to match this row is exactly the imposition brownfield forbids. shadcn is silent on spacing, type, elevation and motion — the system still authors those. The repo's own gate enforces **conformance only**, structurally where the build can (a theme that zeroes the default palette leaves `gray-500` matching nothing) and by test for the rest: a value that isn't in the token file, a class name the sheets don't draw, `--accent` (the hover/selected surface) spent as the brand accent (`--primary`), and a **fill token spent as ink** where the system authored a `-foreground` for it. |
| **The design system's foundation** (what a system needs *before* its values exist: the seven foundations color · typography · size · elevation · motion · primitives · shells, `create` at bootstrap, `audit` over an existing system) → the **`design-system` skill** (repo-authored, `skill/kru-design-system/`; loaded by `ui-designer` on its coverage read, and the user's `/kru-design-system`) | **[Radix scale composition](https://www.radix-ui.com/colors/docs/palette-composition/understanding-the-scale)** for the colour ladder's default step semantics — cited as a *structure* authority, never a palette: the skill takes its step count and what each step is for and declines its hues. Other influences are named per run (`--palette radix`). The rubric itself has no external source — it is the team's, and the entry bar is that a foundation is answered only by a **set**, a **rule** for picking a step, and a **consumer** spending it | Authors **shape, never value** — a hue, a face or a px in a return is the failure mode, since the look is the design turn's and the verdict is the user's. `create` is bootstrap-only and emits `design-system.ts` → `tokens.css` with every value `var(--unset)`; the TS source makes the token file a build product, so **the check script that re-emits and diffs it ships in the same slice** or the source of truth is a suggestion. `audit` never requires `create` to have run, reads a hand-written stylesheet or a Tailwind `@theme` block, and where no step rule is stated **reports the gap rather than inferring one into the file**. The list was cross-checked against Tailwind’s utility-group nav, which is how **elevation** stopped being a clause inside shells; the groups it declines (layout, flex/grid, transforms, filters) are CSS surface with no closed set to settle, so they fail the same bar. Four findings, no fifth (no set · no rule · off-ladder · orphan), ordered by what they block; **no ratio is computed**, here or anywhere on this team. Practice: `${CLAUDE_PLUGIN_ROOT}/references/ui-practice.md` → *Bootstrap* step 3. |
| **The design turn** (settle a look in front of the user, in session: direction artboards, screen mockups, a system change they want to see, print/marketing one-offs) → `ui-designer` drafts the artboards; the **lead** opens them and puts the directions to the user | **`kru-artboards`** (first-party to this fork, `skill/kru-artboards/`) | Upstream's source for this row is claude's bundled `/design` skill — Claude Design's canvas editor, published as a claude.ai artifact the user reshapes and saves. opencode reaches neither that editor nor a claude.ai/design project, so this fork replaces the surface rather than the practice: static `.dc.html` artboards in the tree, opened in a browser, re-drafted on a re-brief. What is lost is direct manipulation and the sync lane; what is kept is the gate — a render in front of the user before any screen is built. |
| **Correctness/quality code review** → `code-reviewer` | **stack-agnostic — no single backing source** → Context7 per detected stack | Pure reviewer: reviews the diff against the repo's own conventions; when a finding hinges on framework/library API behavior, resolve the exact id via Context7 rather than memory. This is the deliberate stack-agnostic exception the wiring map allows for. |
| **Module/interface design + structural integrity** → `architecture-reviewer` | **`codebase-design` skill** + Context7 | Invoke `codebase-design` for the deep-module vocabulary (seams, interface depth, coupling); Context7 for framework-specific seam rules (what belongs in a loader vs component, etc.). Its **`NAVIGATION.md`** is repo-owned rather than vendored and carries the retrieval half — read amplification, naming so a search lands, and the directory topology that sets a change's closure. |
| **Testing** → the **`testing` skill** (ambient — the 8 behavior seats via Block D) + `test-writer` (sweeps, repair, capture) | **the project's own testing skill/docs first**, then the repo-authored **`testing` skill** (`skill/kru-testing/`) + vendored `/tdd` + Context7 (per runner) | Local sources first: read `.claude/skills/*test*` / `TESTING.md` / existing tests + config, and follow them verbatim. `/tdd` for red-green discipline; Context7 for the exact runner's API (`vitest`, `jest`, `playwright`, `@testing-library/*`) — never assert test-API shapes from memory. Vitest's own **"Writing Tests with AI"** guide (`main.vitest.dev/guide/learn/writing-tests-with-ai`) is the first-party upstream for the agent-specific failure modes now folded into the `testing` skill — wrong runner's API from habit, mocks leaking between tests, assertions that only prove the code ran, watch mode hanging the run. They're runner-agnostic (which is why the skill states them without naming a runner); read the page itself only when a Vitest repo's tests keep coming out wrong. If the repo has no written testing knowledge, `test-writer` captures it (its *Capture* step — the skill discovers conventions, the seat writes them down). A repo with `vitest` in `package.json` adds the **`vitest` skill** (next row) under this one. |
| **Vitest** (the runner under `testing`, when the repo has `vitest`) → the **`vitest` skill** (repo-authored, `skill/kru-vitest/`; reached through the `testing` skill's discovery step — conditional, not ambient) | **`https://vitest.dev`** (first-party; every page has a `.md` twin, e.g. `https://vitest.dev/guide/migration.md`; Browser Mode API lives under `/api/browser/*`, its config under `/config/browser/*`) → raw `docs/` in `github.com/vitest-dev/vitest` (`main` = v5, branch `v4` = v4) → **Context7** (`/vitest-dev/vitest`, pin the installed major) | Check the installed major first — the v4/v5 fork table is at the top of the skill; pin Context7 to it. The docs state each default; the skill carries what the run *shows* (the reporter swap, the CI flips, the `mismatched` wording) — read the skill before reading a run. `guide/learn/writing-tests-with-ai.md` is Vitest's own list of what agents get wrong. |
| **Testing Library** (component tests through the accessibility tree, when the repo has `@testing-library/react` or `/svelte`) → the **`testing-library` skill** (repo-authored, `skill/kru-testing-library/`; reached through the `testing` skill's discovery step — conditional, not ambient) | **`https://testing-library.com/docs/`** (first-party; `/queries/about`, `/queries/byrole`, `/dom-testing-library/api-async`, `/user-event/intro` + `/options` + `/keyboard`; raw Markdown under `docs/` in `github.com/testing-library/testing-library-docs`) → jest-dom's README (`github.com/testing-library/jest-dom`, the matcher list) → **Context7** (`/testing-library/testing-library-docs`, `/testing-library/user-event`) | Load the `testing-library` skill for what the queries and interactions *do* — `getByText` never crossing an element boundary under any matcher form, a string `name` as a full match and a regex as a substring, the accessible name over the text (`aria-label`, `aria-hidden`, and the placeholder that names nothing), the input types with no role, `getByRole`'s cost on a big DOM, what jsdom counts as hidden without CSS, jest-dom's `toBeDisabled` over `aria-disabled` and `toHaveValue` on a number input, `waitFor` retrying only on a throw, user-event 14's dropped v13 descriptors and pointer-events check, and the act warning that never prints under `globals: false`. Wiring under Vitest (cleanup, setup file, types, fake timers) stays the `vitest` skill's `reference/dom.md`. Every claim reproduced against `@testing-library/react@16.3.3` + `dom@10.4.1` + `user-event@14.6.7` + `jest-dom@7.0.1` on React 19.2.8; `@testing-library/dom` is a peer of both RTL 16 and jest-dom 7 and is installed explicitly. |
| **Rendered-UI coverage review** → `visual-reviewer` (lead-dispatched, Step 4) **and** the `/visual-review` skill (user-invoked, which spawns that same seat — the browser sweep never blocks the main thread) | **`local-browser` skill** (**`chrome-devtools` MCP**, `ChromeDevTools/chrome-devtools-mcp`) | One body, two callers, same as the row above. Drive the live app via `local-browser` (headless, on a profile that keeps the login; dev server must already be running — never start it). **The server is first-party Chrome tooling and it never touches the user's own browser** — it drives its own instance on its own profile, which is the whole reason it displaced the CLI wrapper: a review sweep has no business raising a window over the work the user is doing. **What the pass is for is the states nobody renders** — empty, error, loading, disabled, focus-visible, 375, the content extremes — and the **cause** behind a visible defect, traced to `file:line` with its scope. The user has already looked at the happy path in their own window and beats any agent at *does this look right*; this seat covers where they never look and does the hour of tracing they'd otherwise do. **Numbers are evidence, not output**: `evaluate_script` settles one doubt at a time and the value goes inline, never a table — tap-target sizes belong to `accessibility-reviewer`, values outside the token file to the repo's conformance gate (already a failing test), and contrast to no pass at all (the design settles it). No verdict on intent, dispatchable or not: conformance to a design is the user's glance. Visual-regression diffing (Percy/Playwright snapshots) is a separate CI concern. |
| **Accessibility (a11y) audit** (WCAG 2.1 AA: keyboard, focus, touch targets, SR, ARIA; post-build — **contrast is the design's and ships as authored**) → `accessibility-reviewer` (lead-dispatched, Step 4) **and** the `/accessibility-review` skill (user-invoked, inline) | **vendored `accessibility-review` skill** (from `anthropics/knowledge-work-plugins` → `design`) | The a11y sibling of the `visual-reviewer` (pixels) pass, and the second of the two **screen passes** that carry a seat — same one-body-two-callers shape. It is a **catch, not the net**: the UI builders carry `ark-ui` and build primitives accessibly in place, so a criterion that keeps failing is a builder-prompt problem and the seat says so. **Color contrast (1.4.3/1.4.11) is the design's and ships as authored** — no seat on this team computes a ratio, so the audit names the two criteria unassessed rather than letting a clean run read as full AA. When a dev server is up, gather **measured** evidence via `local-browser` (`evaluate_script` for `getBoundingClientRect` target size, focus order, focus-indicator computed styles; `lighthouse_audit` as the cheap first sweep, a lead list blind to focus order, keyboard traps and target size) rather than eyeballing; with no server it audits statically from source and marks every rendered criterion **not verified** rather than passed — a fabricated measurement is indistinguishable from a real one downstream. File findings via `TRACKER.md` (user-level plan-store files) when tickets are wanted. Vendored (not the live `design` plugin) so its 9 MCP servers stay out. |
| **UX design** (user research, user flows + IA, flow critique, UX copy, the conventions file in both eras — pre-look corpus, shipped-system header — design→eng handoff spec; **upstream** of the look) → `ux-designer` | **vendored `design`-plugin skills** (from `anthropics/knowledge-work-plugins` → `design`): `user-research`, `research-synthesis`, `ux-copy` | Invoke the matching skill as the playbook: `/user-research` (plan/interview guide), `/research-synthesis` (transcripts→themes — the *usability* lens — the only research lens the team carries), `/ux-copy` (microcopy). The flow critique and the handoff spec are output shapes in the seat itself, not skills. Sits **upstream of the look**, which is `ui-designer`'s to draft and the user's to settle — no seat on this team art-directs, and none extends the design system. No Figma MCP in this team → work from brief/screenshots/repo and whatever the design turn settled, not a live pull. All three are **team-adapted, not verbatim**: the dead `CONNECTORS.md` pointer removed, upstream's Figma-pull instructions replaced, and the tier discipline they shipped without added — reference tokens **by name** rather than inventing a measurement, and **never report a contrast finding**: colour is the design's and no pass on this team computes a ratio (a fill token used where the system authored an ink is the one exception, and it is cited by token name as conformance, never as a ratio). |
| **UX flow audit** (walk a named user flow through source and find usability defects: unlinked destinations, missing states, dead ends, invented affordances) → `ux-auditor` (lead-dispatched, Step 4) **and** the `/ux-review` skill (user-invoked, which spawns that same seat — one body, two callers, with the body in the seat this time and the skill carrying the caller's half) | **repo-authored `ux-principles` skill** (`skill/kru-ux-principles/`) — canon (Krug, Norman, Nielsen, Yablonski, Cooper) with a free, resolving URL on every entry | The seat loads `ux-principles` and walks the flow statically — no browser. **What earns the skill is the `Code signal` field**, not the canon: every model already knows Nielsen's heuristics, so a restatement would fail `skill/kru-roster/author.md`'s entry bar; what no source publishes is what each principle looks like as a violation in component source. The `Detect:` tier (`STATIC`/`HEURISTIC`/`RENDERED`) is load-bearing — it's what stops the auditor reporting a defect it cannot actually see, and one fabricated finding costs the pass its credibility. Journey-scoped, and the seam matters: `/visual-review` reads rendered screens for breakage, `/accessibility-review` audits WCAG conformance, `cro` owns funnel/conversion, `ux-copy` owns the words. Verdicts age slowly, so no aging rule — but every entry carries its source date. |
| **Conversion copy + CRO on shipped pages** (marketing/landing-page copy, copy-editing, page/form conversion) → the **`copywriting`/`copy-editing`/`cro` skills** (user-invoked, run inline) | **vendored marketing skills** (from `coreyhaines31/marketingskills`, copied into `skill/kru-*`): `copywriting`, `copy-editing`, `cro` | The user pulls the pass they want on built pages: `/copywriting` (write/rewrite page copy), `/copy-editing` (seven-sweeps line edit + expert-panel scoring), `/cro` (impact-ordered page/form conversion audit) — builders ship working draft text; copy is cheap to replace post-build. **No first-party source exists for copywriting** — Context7's fallback is for libraries/APIs, not persuasion craft — so a curated community pack from a recognized SaaS-marketing authority (Corey Haines) is the legitimate backing, same tier as the mattpocock pack; this does **not** violate official-sources-first (there is no official source to prefer). Content sibling of the `/seo-review` pass (that owns the meta *markup*; these own the meta *copy*). Distinct from `ux-copy` (in-product microcopy). Vendored (not a live plugin), cross-skill mentions to unvendored channel skills degrade gracefully — see Vendored resources. |
| **Motion/animation craft** (what should animate + how it feels: easing, duration, origin, interruptibility, frequency, gestures/springs) → ambient in the **UI component builders** (`react-ui-builder`, `svelte-ui-builder`) | **vendored Emil Kowalski skills** (from `emilkowalski/skills`, copied into `skill/kru-*`): `find-animation-opportunities`, `emil-design-eng`, `animation-vocabulary`, `review-animations`, `improve-animations` | Build-side pack the builder loads when the plan's motion note/dial (or the component itself) calls for animation: `/emil-design-eng` (decision framework + exact values), `/find-animation-opportunities` (the restraint gate — frequency/purpose/speed/function), `/animation-vocabulary` (name a loosely-described effect). Motion is checked in the user's visual-intent inspection of the built output; `/review-animations` (10 non-negotiable standards + `STANDARDS.md` precise curves/durations) and `/improve-animations` (codebase-wide audit → self-contained fix plans via `AUDIT.md`) remain **user-invoked audits**. **No first-party animation source exists** — Context7 covers libraries, not motion craft — so this authored pack from the reference source (Emil Kowalski: Sonner, Vaul, animations.dev) is the legitimate backing, same posture as the marketing pack; does **not** violate official-sources-first. Motion *personality* is set in the design turn (the brief's motion dial) and captured in the `## Design system` pointer; the builder owns the *precise recipe*. Extend the repo's existing `--ease-*`/`--duration-*` tokens, never invent parallel ones. |
| **The brief + ship cadence** (grill a change to the studs, then persist what lands, how far, in what order, and when it's done) → **`/kru-brief`** (`skill/kru-brief/`, user-invoked, main thread) | **vendored `grilling` skill** + `TRACKER.md` | `/grilling` is the engine (the design tree worked in **rounds** — ask the whole frontier at once, numbered, each with a recommended answer, then wait; facts the agent looks up itself, decisions the user calls); `TRACKER.md` is the format. Output is `plan/<effort>/brief.md` — `Change` · `Why now` · `Blast radius` · **`Landing plan`** (the cadence — commits are the steps of one PR; a second PR means an environment boundary or a partial ship) · `Decisions resolved` · `Non-goals` · `Done when` — rewritten in place per re-grill. **The user is the PM**: no roadmap, no horizons, no prioritization framework, no evidence-gathering pass (replaced the `/pm` pass). Ship cadence lives here rather than upstream because this skill is the only place holding both the grill and the codebase seams. Sits **upstream of `planner`**: brief → spec + tickets. It left the lead's verb list in v0.51.0, following the siblings that went first — all now **self-contained user-invoked skills** with no engine and no source to resolve: **`/kru-todo`** (`skill/kru-todo/`) logs a deferred want as a `pitched` line in the store's `TODOS.md`, scored `value`/`effort` and expanded only from context already loaded, and **`/kru-issue`** (`skill/kru-issue/`) writes an un-investigated `issues/<slug>.md`; **`/kru-todos`** and **`/kru-issues`** (`skill/kru-todos/`, `skill/kru-issues/`) are their read-only halves — list (wants ranked `value ÷ effort`, stale marked), or one entry verbatim, with no triage. This verb reads both artifacts back as grill intake. `TODOS.md`'s exit is **`/kru-todos`**: propose a batch off the ranked file, gate on the user, land it, delete each landed line, rescore what turned out bigger. |
| **Planning / decomposition** (spec, tracer-bullet tickets, wayfinder map for work bigger than one context) → `planner` | **vendored `to-spec` / `to-tickets` / `wayfinder` skills** + `TRACKER.md` | The planner reads the three vendored skills as its playbook (minus their HITL steps — the lead owns those) and persists the plan as **user-level files** under `~/.claude/kru/management/<project-slug>/plan/<effort>/` (`spec.md`/`map.md` + one file per ticket under `tickets/`) via `TRACKER.md`. It reads that effort's `brief.md` as its already-scoped input and inherits its `Landing plan` as the cadence rather than inventing one — the skills' own local-markdown path, made canonical (per-ticket files with `id`/`status`/`blocked_by` frontmatter, edges by id, frontier by query, sequential one-effort-at-a-time). `TRACKER.md` **replaces** `/setup-matt-pocock-skills` for this team — never run that. |
| **Orchestration / harness mechanics** (the `kru` agent's own stack: `task` dispatch + context isolation, `subagent_depth`, worktrees, `todowrite`, resuming a task by `task_id`, model ids and prices, skill invocation, parallel tool calls, the plugin's hooks and permissions) → **`lead`** | **opencode's own docs** (`opencode.ai/docs`; `opencode.ai/docs/zen` for the model catalog and its prices) → **Context7** (`/anomalyco/opencode`, and `/websites/opencode_ai_plugins` for the plugin API) → the binary itself (`opencode models`, `opencode agent list`, `--help`) → **`kru-writing-for-agents`** for any document handed to an agent | The one seat whose stack isn't a framework: everything the lead *does* is harness behavior, so *official sources first* binds it like any builder. Reach for this before asserting a harness fact (what a subagent can spawn, what a `task_id` reuses, what a worktree isolates, where opencode reads config from) and before a **model-tier choice** in a dispatch — "cheap for mechanical checks, capable for judgment" is a routing decision with prices behind it, and this team's own answer is `MODELS.md`. Why a row rather than cached prose in the skill: these docs move, and a cached mechanic goes stale silently while reading identical to a checked one — the failure the corollary above describes. A handoff brief is also an agent document, which is what puts `kru-writing-for-agents` in this chain and not only in the row below. |
| **Authoring skills + agents** (this repo's own maintenance) | **vendored `writing-for-agents` skill** (`skill/kru-writing-for-agents/`; was `writing-great-skills` before mattpocock v1.2.0) | Read it before writing/editing any `agents/*.md` or `skill/kru-*` — context pointers, the two loads (context vs cognitive), information hierarchy + progressive disclosure, completion criteria, leading words, pruning. Its **`SKILL-MECHANICS.md`** carries the skill-only half (frontmatter, model- vs user-invoked, router skills) — read that too whenever the document is a skill. Its **`REFUSALS.md`** is repo-owned rather than vendored and carries the gate-text half — the register a hook's block message or a tool's error text is written in. **Model-invoked** as of v1.2.0, so it also fires on its own when a skill or an `AGENTS.md` is being edited. The standard for "Growing the team" (see `ROSTER.md`). |
| **Astro** *(planned specialist)* | **Context7** (`astro`) + astro docs | Until a dedicated `astro-builder` exists, use the general path + Context7. |

**When a stack has no row:** use Context7 as the fallback, and tell the PM so it can add a row here (and, if the stack recurs, recommend minting a dedicated specialist — see `ROSTER.md`).

## Vendored resources
Official upstream skills copied into this repo (kept verbatim; refresh periodically):
- `skill/kru-react-router/` ← `remix-run/react-router` `main:.agents/skills/react-router` (MIT). Refresh: re-download `SKILL.md` + `references/*.md` from `raw.githubusercontent.com/remix-run/react-router/main/.agents/skills/react-router/`. Symlinked at `~/.claude/skills/react-router`.
- `skill/kru-algorithmic-art/` ← `anthropics/skills` `main:skills/algorithmic-art` (official Anthropic skill; `LICENSE.txt` kept verbatim). The `graphic-designer` seat's code-generated-art path (p5.js generative), alongside its gen-asset script paths. Vendored because it's not installed via a plugin (same reason as `react-router`). Refresh: re-download `SKILL.md` + `LICENSE.txt` + `templates/*` from `raw.githubusercontent.com/anthropics/skills/main/skills/algorithmic-art/`. Symlinked at `~/.claude/skills/algorithmic-art`.
- `skill/kru-{to-spec,to-tickets,wayfinder}/` ← `mattpocock/skills` `v1.2.0:skills/engineering/*` (sha 2ffb184; planner's backing playbook). Kept verbatim; the HITL steps are run by the lead, the tracker substrate is supplied by `TRACKER.md` (not `/setup-matt-pocock-skills`). Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/engineering/{to-spec,to-tickets,wayfinder}/SKILL.md`. Symlinked at `~/.claude/skills/{to-spec,to-tickets,wayfinder}`.
- `skill/kru-accessibility-review/` ← `anthropics/knowledge-work-plugins` → `design` plugin (v1.2.0, sha 15898ec). A team-adapted copy (connector hooks remapped to `local-browser`/`TRACKER.md`). Vendored so the `design` plugin's 9 MCP servers stay out. Symlinked at `~/.claude/skills/accessibility-review`.
- `skill/kru-{user-research,research-synthesis,ux-copy}/` ← `anthropics/knowledge-work-plugins` → `design` plugin (v1.2.0, sha 15898ec). The `ux-designer` seat's playbook. Vendored so the `design` plugin's 9 MCP servers stay out. Each carries a provenance header; symlinked into `~/.claude/skills/`. **Team-adapted, not verbatim** — connector hooks remapped and tier discipline added; see the UX-design row above for what changed and why, and re-apply after any re-sync.
- ~~`skill/kru-{roadmap-update,synthesize-research,competitive-brief,metrics-review,stakeholder-update,product-brainstorming}/`~~ ← `anthropics/knowledge-work-plugins` → `product-management` plugin. **Deleted** with the `/pm` pass: the team has no product-management function (the user is the PM), so nothing routed them. Re-vendor from the plugin if that ever changes.
- `skill/kru-{copywriting,copy-editing,cro}/` ← `coreyhaines31/marketingskills` (`main`, sha 286d371; SKILL.md + `references/*`, verbatim). **User-invoked copy passes** run inline in the main thread. A **community pack**, vendored (unlike the first-party sources above) because no official copywriting source exists — Corey Haines is a recognized SaaS-marketing authority, so this is a vetted known-good pack, same tier as the mattpocock engineering skills. Only these three of the repo's ~47 skills are taken (the on-page, ships-in-the-build ones); `image`/`video` are already `graphic-designer`'s, the SEO cluster is the `/seo-review` pass's lane (backed by `sanity:seo-aeo-best-practices`, not this pack), and channel/campaign skills stay out of scope. Cross-skill mentions to unvendored skills (`emails`/`popups`/`offers`/`ab-testing`/`marketing-psychology`) degrade gracefully. Each carries a provenance header; symlinked into `~/.claude/skills/`. Refresh: re-download from `raw.githubusercontent.com/coreyhaines31/marketingskills/main/skills/{copywriting,copy-editing,cro}/`.
- `skill/kru-{find-animation-opportunities,emil-design-eng,animation-vocabulary,review-animations,improve-animations}/` ← `emilkowalski/skills` (`main`, sha 6bf2443; SKILL.md + aux `AUDIT.md`/`PLAN-TEMPLATE.md`/`STANDARDS.md`, verbatim; MIT `LICENSE` per dir). The UI component builders' (`react-ui-builder`/`svelte-ui-builder`) ambient motion playbook; `review-animations`/`improve-animations` double as user-invoked audits. An **authored pack** from the reference web-animation source (Emil Kowalski — Sonner, Vaul, animations.dev), vendored (like the marketing pack, unlike the first-party sources above) because no official animation source exists — a vetted known-good pack, same posture. All six of the repo's skills are taken (all are motion craft). `review-animations` keeps `disable-model-invocation: true` (user-invoked). Each `SKILL.md` carries a provenance header; symlinked into `~/.claude/skills/`. Refresh: re-download from `raw.githubusercontent.com/emilkowalski/skills/main/skills/{find-animation-opportunities,emil-design-eng,animation-vocabulary,review-animations,improve-animations}/`.
- `skill/kru-turborepo/` ← `vercel/turborepo` `main:skills/turborepo` (MIT; `LICENSE` kept verbatim). The `toolchain-engineer` seat's monorepo task-graph/caching playbook (SKILL.md + `command/` + `references/`). Vendored (not plugin-installed) like `react-router`; the pnpm + Biome halves use official docs, not vendored. Refresh: re-download from `raw.githubusercontent.com/vercel/turborepo/main/skills/turborepo/`. Symlinked at `~/.claude/skills/turborepo`.
- `skill/kru-shadcn/` ← `shadcn-ui/ui` `main:skills/shadcn` (sha efac598, MIT). shadcn's own first-party skill — the `react-ui-builder` seat's second primitive library, alongside `ark-ui`. Kept verbatim minus `agents/openai.yml` (Codex metadata), `evals/` (fixtures) and `assets/*.png` (branding); none is referenced by the prose. **No frontmatter port needed** — `user-invocable: false`, `allowed-tools`, and the `` !`cmd` `` injection are all native Claude Code fields, so the project-context block genuinely executes. Vendored rather than installed per-project with `pnpm dlx skills add shadcn/ui` for the reason the whole repo is a plugin: `~/.claude` doesn't exist on the web VM. Refresh: re-download `SKILL.md` + `cli.md` + `customization.md` + `mcp.md` + `registry.md` + `rules/*.md` from `github.com/shadcn-ui/ui/tree/main/skills/shadcn`, then re-add the provenance comment.
- `skill/kru-cloudflare/` + `skill/kru-wrangler/` ← `cloudflare/skills` `main` (Apache-2.0; `LICENSE` kept verbatim). The `cloudflare-builder` seat's platform + CLI playbook — `cloudflare` is the comprehensive skill (SKILL.md indexes ~60 product `references/*`, loaded on demand), `wrangler` the CLI skill. Vendored the two user-facing core skills; the specialized siblings (`durable-objects`, `agents-sdk`, `sandbox-sdk`, `workers-best-practices`, `cloudflare-one`) stay in the upstream repo, installable via the CF plugin/`npx skills` if a project needs them. Works alongside the connected Cloudflare MCP. Refresh: re-download `skill/kru-{cloudflare,wrangler}/` from the `cloudflare/skills` repo. Symlinked at `~/.claude/skills/{cloudflare,wrangler}`.

- `skill/kru-webgpu-threejs-tsl/` ← `dgreenheck/webgpu-claude-skill` (`main`, sha af2319b; `SKILL.md` + `REFERENCE.md` + `docs/*` + `examples/*` + `templates/*`, verbatim). The shader/compute half of the **3D lane**, carried ambiently by the UI component builders. MIT **per the source README — no `LICENSE` file upstream**; its examples derive from three.js (MIT). Same no-per-dir-LICENSE precedent as the mattpocock skills. Unlike the marketing/animation packs, this one backs a domain that *does* have a first-party source, so it is scoped deliberately narrow: **TSL / WebGPU / node materials / GPU compute only**, with Context7 (`/llmstxt/threejs_llms-full_txt`, `/mrdoob/three.js`, `/threlte/threlte`, R3F) outranking it on core three and the component layer — see the 3D row above. Taken over the larger `cloudai-x/threejs-skills` pack (2.8k stars), which was **rejected**: no LICENSE at all, unmaintained since Jan 2026, self-audited only to r160 (~2 years stale), 10 flat 12–16KB files with no progressive disclosure, vanilla-imperative three with zero R3F/Threlte — and it duplicates docs Context7 already serves current. This one is maintained (r183+ as of Apr 2026) and structured as `SKILL.md` → `docs/`/`examples/`/`templates/` on demand. Refresh: re-download from `raw.githubusercontent.com/dgreenheck/webgpu-claude-skill/main/skills/webgpu-threejs-tsl/`.

Reuse/maintenance skills — **vendored** (was "reused from `~/.claude/skills`"); the team ships one copy pinned to its own version rather than reading whatever a machine happens to have:
- `skill/kru-{codebase-design,tdd,diagnosing-bugs,domain-modeling}/` ← `mattpocock/skills` `v1.2.0:skills/engineering/*` (sha 2ffb184). `codebase-design` (deep-module vocabulary, backs `architecture-reviewer`), `tdd` (backs `test-writer` + the lead's test-first loop), `diagnosing-bugs` (backs `vercel-perf-optimizer` + the lead's debug loop), `domain-modeling` (shared). Bodies verbatim; no per-dir LICENSE (mattpocock ships only a root LICENSE — same precedent as `to-spec`/`to-tickets`/`wayfinder`). `codebase-design` deviates in one line — the pointer under *Going deeper* to **`NAVIGATION.md`**, the repo-owned retrieval branch (read amplification, naming for search, directory topology) upstream has no section for; a re-sync re-applies that line and `NAVIGATION.md` survives untouched. Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/engineering/{codebase-design,tdd,diagnosing-bugs,domain-modeling}/`. Symlinked at `~/.claude/skills/{codebase-design,tdd,diagnosing-bugs,domain-modeling}`.
- `skill/kru-{grilling,writing-for-agents}/` ← `mattpocock/skills` `v1.2.0:skills/productivity/*` (sha 2ffb184). `grilling` (plan/design stress-test — the engine under the lead's `brief` verb + Step 2.5 grilling; also backs `planner`'s input), `writing-for-agents` (`SKILL.md` + `SKILL-MECHANICS.md`; the authoring standard for growing the team — **renamed from `writing-great-skills`** in upstream v1.2.0, with `GLOSSARY.md` merged into `SKILL.md` and the skill-only mechanics disclosed to `SKILL-MECHANICS.md`; now model-invoked, no alias upstream). Both bodies verbatim, no per-dir LICENSE; `writing-for-agents` deviates in three places — two in its `description` (the `agents/*.md` branch upstream's omits, so seat authoring actually fires it, and a **discovery branch**: does a repo fact just learned earn a line in an always-loaded file, without which the pointer only fires after the decision to write is already made), and one line in the body pointing at **`REFUSALS.md`**, the repo-owned refusal-register reference (sourced from `mksglu/context-mode` ADR-0002 + ADR-0003) that upstream has no branch for; a re-sync has to re-apply all three, and `REFUSALS.md` itself survives one untouched. Refresh from `raw.githubusercontent.com/mattpocock/skills/main/skills/productivity/{grilling,writing-for-agents}/`. Symlinked at `~/.claude/skills/{grilling,writing-for-agents}`.

**Excluded from all nine mattpocock skills: the `agents/openai.yaml` Codex metadata** upstream v1.2.0 added beside every `SKILL.md`. It carries Codex UI display names and `policy.allow_implicit_invocation`; this team runs on Claude Code only, so the file is dead weight here. Re-add it if a Codex harness is ever added.
- `skill/kru-local-browser/` ← **locally authored** (this repo), a thin wrapper over the **`chrome-devtools` MCP server** (`ChromeDevTools/chrome-devtools-mcp`, first-party Chrome) that backs the `/visual-review` and `/accessibility-review` rendered checks. No upstream skill exists → no re-sync source; maintain it here. **It carries only what the tool schemas don't say** — the ~500px `resize_page` clamp, `emulate` replacing whole state rather than patching it, and the profile that turns one interactive login into every later run's session. Each was verified by driving the server (chrome-devtools-mcp@1.7.0, 2026-08-24); a server upgrade re-opens them. Replaced the `agent-browser` CLI wrapper in v0.23.0.

Install copies the whole tree into the opencode config dir (`bin/kru-oc.mjs`, see README) — the "Symlinked at `~/.claude/skills`" notes in the entries above are legacy from a local install and describe nothing this fork does.

Svelte needs no vendoring — its official AI surface (MCP, `svelte:*` skills, `svelte:svelte-file-editor` subagent) is already installed; llms fallback at `svelte.dev/llms.txt`.
